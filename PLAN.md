# PLAN.md — خطة تنفيذ المرحلة ٠ (المتبقّى) والمرحلة ١

> مُشتقّة بالكامل من `CLAUDE.md` — لا تواريخ ولا تقديرات غير مذكورة هناك أُضيفت هنا.
> **حدّث هذا الملف كلما تغيّر الترتيب أو اكتملت خطوة** (نفس بروتوكول تحديث `CLAUDE.md`).

---

## 🔴 التبعية الأهم: نصف المرحلة ١ مستحيل قبل الباك اند

اليوم (2026-09-21) **لا شىء من التالى موجود فى الباك اند**: لا جداول (`drivers`, `driver_restaurant_links`,
`fleets`, `driver_tokens`, `driver_offers`, `driver_ledger_entries`, `driver_settlements`,
`trip_breadcrumbs`, `order_delivery_events`)، ولا أى مسار تحت `/api/v1/driver/*`، ولا
`PlanFeature::DeliveryManagement`. كل ما هو مذكور فى قسم «✅ الموجود فعلاً فى الباك اند» هو **بنية عامة
للطلبات/السائقين القدامى** (`delivery_driver`, `DeliveryDrivers` resource, `PosStaffToken` كنمط) —
وليس أياً من عقد `/driver/*` الجديد.

**النتيجة العملية:** أى خطوة فى هذا المستند تحمل الوسم **[يحتاج باك اند]** لا يمكن أن "تكتمل" فعلياً
(بمعنى: شاشة تعمل بنداء حقيقى) حتى يبنيها وكيل آخر فى ريبو الباك اند، بالترتيب المطابق لجدول
«ما يتغيّر فى الباك اند» فى `CLAUDE.md`. **لا تكتب "تمّت" على خطوة تطبيق تعتمد على endpoint غير موجود
بعد — أقصى ما يمكن قوله هو "الطبقة المحلية جاهزة، بانتظار الخادم".**

ترتيب بناء الباك اند نفسه (من `CLAUDE.md`، قسم «ما يتغيّر فى الباك اند» + جدول أسابيع المرحلة ١):
1. الجداول السبعة + `Driver`/`DriverRestaurantLink`/`DriverToken`/`Fleet` (الأسبوع ١، الأساس الذى يعتمد عليه كل شىء آخر).
2. `DriverAuthService` + `driver/auth/*` + `driver/me` (الأسبوع ١) — **يفتح باب تسجيل دخول التطبيق**.
3. `DriverLocationService` (Redis) + `driver/availability` + `driver/location` (الأسبوع ٢).
4. إعدادات الفرع (`driver_assignment_mode`, `driver_dispatch_timing`, …) + `Delivery\DispatchPolicy` (الأسبوع ٣) — **أول نقطة يمكن عندها تجربة دورة رحلة كاملة يدوياً**.
5. `Delivery\DriverAssigner` + `driver_offers` + ACK/إعادة التوزيع + `Fcm\FcmService` (الأسبوع ٤).
6. `driver/orders/{id}/{picked-up,delivered,issue}` + `order_delivery_events` + `TripBreadcrumb` (الأسبوع ٥).
7. `DriverLedgerService` + `driver_ledger_entries`/`driver_settlements` + `driver/ledger*` (الأسبوع ٦).
8. `PlanFeature::DeliveryManagement` (New-Feature Checklist كاملاً) + لوحة الشركة (الأسبوع ٧).
9. `driver/app-version` + `driver.client` middleware (الأسبوع ٨).
10. تصليب: عزل المستأجرين، idempotency، `route:cache`، الخصوصية (الأسبوع ٩–١٠).

**كل خطوة تطبيق أدناه مربوطة برقم من هذه القائمة.**

---

## معلّق على صاحب المشروع — يحجب خطوات بعينها

من قسم «📋 ما يحتاجه صاحب المشروع» فى `CLAUDE.md`:

| البند | يحجب |
|---|---|
| مشروع Firebase (Phone Auth) + خطة Blaze | **كل** شاشات الدخول الحقيقى (OTP) — بدونه شاشة الدخول تُبنى لكن لا تُختبَر end-to-end |
| مفتاح توقيع (keystore) | أى بناء `release`/`staging` موقَّع؛ `debug` لا يحتاجه |
| مفتاح خرائط جوجل | زر الملاحة الخارجية (`google.navigation:q=`) لا يحتاجه فعلياً — لكن أى Static Maps لاحقاً يحتاجه |
| Sentry DSN | تسجيل الأعطال فى الإنتاج (الأسبوع ٧) — التطوير المحلى يعمل بلا DSN (SDK صامت) |
| فرع تجريبى بإحداثيات + مناطق توصيل + عنوانا عميلين | **الاختبار الميدانى الحقيقى للدورة الكاملة** (الأسبوع ٣ فصاعداً) — بدونه لا بيانات حقيقية فى `NewdemoSeeder` |
| رقم جوال حقيقى لاختبار OTP | اختبار OTP الحقيقى (Firebase Phone Auth) |
| هاتف رخيص | البيتا المغلقة (الأسبوع ٩–١٠) فقط — لا يحجب التطوير المبكر |
| أول مطعم مستخدم | لا يحجب البناء، لكنه المعيار الوحيد لـ«القبول النهائى» |

**الأثر الأكبر:** غياب Firebase يعنى أن دورة الدخول الكاملة (خطوة ١.٢ أدناه) تبقى غير قابلة للتحقق حتى
تُنشأ خطة Blaze — يمكن بناء الشاشات ومحاكاة الاستجابة، لكن "تمّ" الحقيقى ينتظر هذا البند تحديداً.

---

## المرحلة ٠ — المتبقّى (الخطوات ٣–٦ من ٦)

الحالة الحالية (من `CLAUDE.md`): الخطوتان ١ (Gradle + `app` بشاشة تحقّق مؤقتة) و٢ (`core:designsystem`)
مكتملتان ومُتحقَّق منهما على الجهاز.

### خطوة ٠.٣ — `core:common` + `core:datastore`
**ما يلزم:** نسخ `LocaleManager` + `AppLocaleStore` من POS بخمس لغات (`ar,en,ur,bn,hi`) + أرقام لاتينية
(`-u-nu-latn`)، و`TokenStore` على `EncryptedSharedPreferences`.
**تعريف «تمّ»:**
- `./gradlew :core:common:assembleDebug :core:datastore:assembleDebug` ناجح.
- اختبار وحدة يثبت أن `AppLocaleStore` يحفظ/يستعيد كل لغة من الخمس.
- `TokenStore` يكتب/يقرأ توكناً وهمياً من `EncryptedSharedPreferences` فعلياً (لا mock) فى اختبار.
- **لا يحتاج باك اند.**

### خطوة ٠.٤ — `core:network`
**ما يلزم:** نسخ `NetworkModule` + الـinterceptors (وأهمّها `DnsRetryInterceptor`) من POS، مهيّأ لقاعدة
`/api/v1/driver/*` (النكهة تحدّد الدومين عبر `Flavor.kt` الموجود أصلاً).
**تعريف «تمّ»:**
- الوحدة تُبنى وتحقن Retrofit/OkHttp عبر Hilt بلا خطأ (`hiltJavaCompile…`).
- `BackendContractMirrorTest` موجود ويقرأ `core/network/src/test/resources/contract/driver.v1.json` (حتى لو كان الملف بعقد أوّلى فارغ فى هذه الخطوة — يُملأ فعلياً فى خطوة ٠.٦).
- **[يحتاج باك اند لاحقاً]** ليعمل بنداء حقيقى — لكن بناء الوحدة نفسه لا يحتاجه.

### خطوة ٠.٥ — `.claude/skills/` (السبع)
**ما يلزم:** نظير مهارات POS السبع (add-api-endpoint, add-compose-screen, add-feature-module,
add-room-entity, pos-ui-standards, update-claude-md, wire-hilt-module) مُعاد تسميتها/تكييفها لهذا الريبو.
**ملاحظة تنسيق:** وكيل آخر مكلَّف بهذا الملف فعلاً (مذكور فى تكليف هذا الوكيل) — لا يُبنى هنا، فقط يُذكر كتبعية توثيقية فى `PLAN.md`.
**تعريف «تمّ»:** كل مهارة تحمل نفس بنية frontmatter القابلة للتشغيل، ومُختبَرة بتشغيلها فعلياً على مهمة تجريبية واحدة (لا قراءة فقط).

### خطوة ٠.٦ — `scripts/verify.sh` + `PLAN.md` + `openapi/driver.v1.yaml`
**الحالة:** الثلاثة أُنشئت فى هذه الجلسة. **`openapi/driver.v1.yaml` مكتوب** (٢٦ مساراً · ٢٦ عملية · ١٦ مخطّطاً ·
٩٤ مرجعاً كلها تُحَلّ — مُتحقَّق بتحليل الملف فعلياً لا بالنظر). يحمل رأسه صراحةً أنه **العقد المنوى** وأن
**مصدر الحقيقة ينتقل لريبو الباك اند** حين يُبنى (سابقة `openapi/pos.v1.yaml` + اختبار عقد يرسل طلبات حقيقية)،
وأن هذا الملف يصير **المرآة** — فلا ينشأ عقدان يتباعدان.
**تعريف «تمّ» لبقية هذه الخطوة (المتبقّى):** مرآته فى `core/network/src/test/resources/contract/driver.v1.json`
مُولَّدة ومطابقة (يحرسها `BackendContractMirrorTest`) — **لا يمكن إنجازها قبل `:core:network`** (خطوة ٠.٤).

### خطوة ٠.٧ — شاشتا اللغة والدخول
**ما يلزم:** شاشة ١ (اختيار اللغة، أول تشغيل بلا افتراض) وشاشة ٢ (الدخول بالجوال، مبدّل لغة ظاهر) من
«خريطة الشاشات». هذه الشاشتان **واجهة فقط فى هذه المرحلة** — إرسال الجوال الفعلى (`request-otp`) هو
[يحتاج باك اند: بند ٢ فى قائمة الترتيب].
**تعريف «تمّ» (مقبول القبول الرسمى للمرحلة ٠):**
- `verify.sh` أخضر (الاثنتان: البناء + الاختبارات).
- التطبيق يفتح على S25 بالنكهتين.
- اللغات الخمس تُعرض فعلياً فى شاشة الاختيار بأرقام لاتينية.
- الوضعان الفاتح والداكن مُتحقَّقان بقياس البكسل (نفس منهج `core:designsystem`).
- تكبير خط النظام حتى 200٪ بلا كسر تخطيط على شاشتى اللغة والدخول تحديداً.
- **لا يشترط عمل OTP فعلياً** — ذلك ينتظر الباك اند (بند ٢) وFirebase Blaze معاً.

---

## المرحلة ١ — MVP (بالترتيب الحرفى من `CLAUDE.md`، جدول الأسابيع)

كل صف: الجزء المحلى (تطبيق) مقابل تبعيته فى الباك اند، وتعريف «تمّ» قابل للملاحظة.

### ١.١ (أسبوع ١) — الدخول: OTP + الرمز + قائمة المطاعم
- **[يحتاج باك اند: بند ٢]** `driver/auth/{request-otp,verify-otp,set-pin,login,redeem-invite,logout}` + `driver/me`.
- **تطبيق:** شاشات ٣–٥ (OTP، ضبط الرمز، كود الدعوة) + تخزين التوكن على `driver_id` عبر `TokenStore` (خطوة ٠.٣).
- **تعريف «تمّ»:** تسجيل دخول حقيقى (لا mock) من جهاز حقيقى بجوال سائق مدعوّ فعلاً، يُرجِع قائمة مطاعمه، ويُخزَّن التوكن مُشفَّراً. اختبار عزل: توكن سائق لا يرى بيانات سائق آخر (طرف الباك اند).
- **معلّق:** Firebase Blaze (بند «معلّق على صاحب المشروع») — بدونه هذه الخطوة تُبنى لكن لا "تمّ" حقيقياً.

### ١.٢ (أسبوع ٢) — التوفّر + الموقع
- **[يحتاج باك اند: بند ٣]** `driver/availability` (PATCH) + `driver/location` (POST) + Redis GEO + sweep + بثّ.
- **تطبيق:** شاشة ٨ (الرئيسية: متاح/غير متاح) + `ForegroundService` نوع `location` + الإرسال المجمَّع (15ث) + heartbeat + شاشة دليل البطارية (١٦).
- **تعريف «تمّ»:** الجهاز فى الجيب و«متاح» لمدة ٣٠–٦٠ دقيقة بلا انقطاع، ثم فحص `last_seen_at` فى MySQL (لا محاكى — انظر قسم المخاطر فى `CLAUDE.md`).

### ١.٣ (أسبوع ٣) — إعدادات الفرع + `DispatchPolicy` + التوقيتات
- **[يحتاج باك اند: بند ٤]** قسم «التوصيل بالسائقين» فى `BranchForm` + `Delivery\DispatchPolicy` + التوقيتات الأربعة (`driver_dispatch_timing`).
- **تطبيق:** قائمتا الطلبات (متاحة/طلباتى) + بطاقة الطلب + عدّاد «جاهز خلال» + طابور محلى (Room، `driver_actions_outbox`).
- **✅ علامة مرجعية صريحة فى `CLAUDE.md`:** «أول نسخة تُجرَّب على جوالك: دخول + متاح + طلبات حقيقية + دورة رحلة».
- **تعريف «تمّ»:** طلب حقيقى فى الباك اند يظهر فى قائمة «المتاحة» على الجهاز خلال المهلة التى يحددها إعداد الفرع.

### ١.٤ (أسبوع ٤) — الإسناد + العرض + ACK
- **[يحتاج باك اند: بند ٥]** `Delivery\DriverAssigner` + `driver_offers` + `driver/orders/{id}/{ack,accept,decline,claim,release}` + `Fcm\FcmService`.
- **تطبيق:** شاشة ٩ (العرض كامل الشاشة، عدّاد ٤٥ث، صوت متواصل، اهتزاز) + قبول/رفض/التقاط.
- **تعريف «تمّ»:** عرض يصل عبر FCM data-only + استطلاع 15ث كشبكة أمان، ACK مسجَّل خادمياً، قبول يقفل الطلب ذرّياً (لا سباق بين مطعمين — الاختبار يحاكى طلبين متزامنين).

### ١.٥ (أسبوع ٥) — الرحلة كاملة
- **[يحتاج باك اند: بند ٦]** `driver/orders/{id}/{picked-up,delivered,issue,breadcrumbs}` + `order_delivery_events` + إشعارات الإلغاء.
- **تطبيق:** شاشات ١٠–١٢ (تفاصيل/الرحلة، التسليم بالنقد، تسجيل مشكلة).
- **تعريف «تمّ»:** دورة كاملة تنتهى بـ`Order::transitionTo(Delivered)` + `delivery_cash_collected` مسجَّل + `order_delivery_events` يحوى كل الأحداث بالترتيب الصحيح.

### ١.٦ (أسبوع ٦) — الدفتر والمقاصّة
- **[يحتاج باك اند: بند ٧]** `Delivery\DriverLedgerService` + `driver_ledger_entries`/`driver_settlements` + `driver/ledger*` + `driver_fee` كلقطة.
- **تطبيق:** شاشة ١٣ (الدفتر: كسبتَ اليوم / النقد بحوزتك / الصافى، لكل مطعم).
- **تعريف «تمّ»:** مثال اليوم فى `CLAUDE.md` (5 فيزا + 3 نقدية ⇒ صافى −19) يُعاد إنتاجه فعلياً باختبار، وقيد كل عملية يحمل مفتاح idempotency (`order_id + type`).

### ١.٧ (أسبوع ٧) — بوّابة الخطة + التدريب
- **[يحتاج باك اند: بند ٨]** `PlanFeature::DeliveryManagement` (**New-Feature Checklist كاملاً** — 7 خطوات كما فى قسم الـchecklist فى CLAUDE.md الرئيسى للباك اند) + لوحة الشركة.
- **تطبيق:** شاشة ١٤ (السجلّ، ٧ أيام) + ١٥ (الإعدادات) + ٦ (التدريب) + تكامل Sentry (يحتاج DSN من صاحب المشروع).
- **تعريف «تمّ»:** شركة بلا الميزة تُرفض بـ`feature_not_in_plan` فى `driver.auth`؛ شركة بها الميزة تعمل كاملة.

### ١.٨ (أسبوع ٨) — المُحدِّث الذاتى
- **[يحتاج باك اند: بند ٩]** `driver/app-version` + `driver.client` middleware (`app_update_required`).
- **تطبيق:** شاشة ١٧ (التحديث الإجبارى، شاشة كاملة) + بانر التحديث الاختيارى.
- **تعريف «تمّ»:** رفع `versionCode` على الخادم يمنع/يذكّر بحسب `min_version_code`/`latest_version_code` (مقارنة عددية لا نصّية — القاعدة المجمَّدة فى `CLAUDE.md`).

### ١.٩–١.١٠ (أسبوعان ٩–١٠) — التصليب + البيتا
- **[يحتاج باك اند: بند ١٠]** عزل المستأجرين، idempotency على كل أمر رحلة، `route:cache`، مراجعة الخصوصية.
- **تطبيق:** شاشة ١٨ (جهاز غير مدعوم) + مراجعة الترجمات الثلاث المتبقية (ur/bn/hi) من ناطق أصلى (قاعدة صريحة فى `CLAUDE.md`: "لا تُطلَق بلا مراجعة").
- **تعريف «تمّ» (القبول النهائى الحرفى من `CLAUDE.md`):** دعوة سائق → دخول بجواله → متاح → طلب يُؤكَّد → يصل بالوقت والطريقة اللذين يحددهما الفرع (≤5ث) → ACK مسجَّل → قبول → استلمت (بعد Ready) → العميل يرى «فى الطريق» → سلّمت (+نقد) → `delivered` + قيود الدفتر صحيحة + التسوية تُصفّر الرصيد → الجهاز فى الجيب 60 دقيقة بلا انقطاع.
- **بيتا ميدانية:** 3–5 سائقين على **هاتفين رخيصين** (بند "هاتف رخيص" فى قائمة صاحب المشروع) لمدة أسبوعين — لا بديل لهذا الاختبار.

---

## عمداً خارج نطاق هذه الخطة (من `CLAUDE.md` مباشرة)

**ليس فى المرحلة ١:** ETA عبر API · موجات متعددة · تجميع · OTP تسليم · صورة إثبات · تقييم · خريطة حيّة
للمطعم · تتبّع العميل للسائق · كشف أجور · iOS.

**لا تبنِ اليوم** أى من: اختيار السائق لدولته/منطقته (قرار ٤٥–٤٦؛ يخصّ المرحلة ٤ فقط)، محرك أولويات
توزيع معقّد (القفل الذرّى + سقف طلب واحد يغنيان عنه فى المرحلة ١)، Play In-App Updates (لا تعمل مع APK
مُثبَّت يدوياً).

---

## تبعيات بين خطوات المرحلة ١ غير مذكورة صراحةً فى `CLAUDE.md` — استُنتجت هنا

1. **`driver_offers` (١.٤) لا معنى لبنائها قبل `DispatchPolicy` (١.٣)** — العرض هو نتاج قرار التوزيع، والقفل الذرّى فى قسم «القفل» يفترض وجود مرشّحين محسوبين مسبقاً من `DispatchPolicy`.
2. **شاشة الدفتر (١.٦) تعتمد على `driver_fee` كلقطة تُختم "وقت إنشاء الطلب"** — أى أن حقل `orders.driver_fee` يجب أن يُضاف **قبل** أى طلب تجريبى يُستخدَم لاختبار الدفتر لاحقاً، وإلا كانت الطلبات القديمة بلا لقطة فيفشل حساب المقاصّة عليها بصمت. يستحق تنويهاً صريحاً فى تسليم الأسبوع ١ للباك اند حتى لو لم يُستخدَم إلا فى الأسبوع ٦.
3. **`Fleet` (`fleets.type=restaurant`) يجب أن يوجد من الأسبوع ١** رغم أنه لا يُستهلَك فعلياً فى واجهة المستخدم قبل المرحلة ٤ — لأن `driver_restaurant_links` قد يحتاج الإشارة إليه لاحقاً بلا هجرة كاسرة (نفس منطق "لا تبنِ اليوم ما يستحيل تمديده غداً" الوارد فى قرار ٤٥–٤٧). إن تأجّل بناء `Fleet` لأسبوع لاحق، تأكد أن `driver_restaurant_links.company_id` وحده يكفى لبناء الأسبوعين ١–٣ بلا الرجوع لتعديل مخطّط قائم.
4. **~~شاشة «لا طلبات» (قرار ٤٧) بلا بيانات~~ — أُغلقت فى نفس الجلسة.** كانت الملاحظة صحيحة تماماً: السبب تركيبة من
   `driver_restaurant_links.branch_ids` + حالة الفرع + حساب مسافة، ولا يجوز أن يخمّنها التطبيق من **غياب** البيانات.
   أُضيف `AvailabilityContext` إلى استجابة `GET driver/orders/available` فى العقد: `is_online` · `has_active_trip` ·
   **كل الفروع المرتبطة بالاسم** مع `is_open`/`distance_km`/`within_radius` · `search_radius_km` · و`reason` من سبعة
   (`offline` · `no_active_link` · `all_branches_closed` · `outside_radius` · `location_unknown` · `has_active_trip` ·
   `nothing_pending`). 🔑 **الخادم هو من يسمّى السبب** لأنه وحده يعرف نصف القطر وقواعد الترشيح، و`nothing_pending`
   (الحالة الصحّية) يجب أن تُقرأ فى الواجهة **مختلفةً** عن `outside_radius` الذى يملك السائق إصلاحه بنفسه.
5. **اختبار "طلبين متزامنين يعرضان لنفس السائق" (١.٤) يحتاج بيانات فرع بإحداثيات حقيقية** — وهذا مرتبط ببند "فرع تجريبى بإحداثيات" المعلّق على صاحب المشروع. لا يمكن كتابة هذا الاختبار الميدانى (لا اختبار الوحدة) بلا هذا البند، رغم أن الكود نفسه لا يعتمد عليه مباشرة.
