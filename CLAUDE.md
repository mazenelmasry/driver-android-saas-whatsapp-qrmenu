# CLAUDE.md — تطبيق السائقين (Android أصلى — Kotlin + Jetpack Compose)

> **لأى وكيل ذكاء اصطناعى يعمل فى هذا الريبو.** يُحمَّل تلقائياً فى كل جلسة. اقرأه كاملاً قبل أول سطر كود.
> المشروع يُبنى **بالكامل بوكلاء برمجة بلا مبرمج بشرى** — فالقواعد هنا ليست تفضيلات بل الحارس الوحيد.
> **كل قرار فيه حسمه صاحب المشروع فى جلسة 2026-09-21.** لا تُعِد فتح قرار محسوم؛ إن رأيت خطأً فاعرضه عليه ولا تغيّره من تلقاء نفسك.

---

## 🔚 تسليم الجلسة — اقرأه أولاً وحدّثه أخيراً

> **هذا القسم هو أول ما تقرأه وآخر ما تكتبه فى كل جلسة. لا استثناء.**

### 📍 أين وصلنا الآن
| | |
|---|---|
| **آخر تحديث** | 2026-09-21 (جلسة سادسة) |
| **المرحلة** | **٠ مكتملة** · **المرحلة ١: الأسابيع ١–٥ + ٧ مكتملة** · الدفتر المالى (٦) سُحب للأمام — التالى: الأسبوع ٨ |
| **آخر commit** | انظر أسفل هذا القسم (جلسة ٦ غير مدفوعة حتى يوافق صاحب المشروع) |
| **الشجرة** | الباك اند والسائقون: عمل جلسة ٦ مُلتزَم **وغير مدفوع**. استثناءان **لا يخصّان السائقين ولم يُلمسا عمداً**: الباك اند `lang/{ar,en}/roles.php` و`lang/ar/validation.php` (تنسيق pint قديم) · الفرونت `components/landing/PricingSection.tsx` (شغل جلسة أخرى) |
| **البناء** | ✅ النكهتان · **تاج يُبنى بلا Firebase** (وضع `:core:push` يتدهور بأمان) |
| **الجهاز** | ✅ S25 (أندرويد ١٦) · **الأسبوع ٧ مُثبَت كاملاً — انظر الجدول أدناه** |
| **الاختبارات** | أندرويد **٤٨٨** (كانت ٤١٤؛ `:core:push` ١٧ · `:app` ٣٥) · الباك اند **١٩٩ اختبار سائق** (كانت ١٨٧) · `verify.sh` أخضر على النكهتين |

### ✅ أسبوع ٧ — الإشعارات الحيّة (FCM) — مُثبَت على جهاز حقيقى

**🔑 الحاجز المُعلَن فى الجلسة السابقة كان خاطئاً: FCM لا يحتاج بصمة SHA-1 إطلاقاً** — البصمة لـPhone Auth وحده. `app/src/meniura/google-services.json` كان موجوداً و`app.qrmenu.driver.meniura.debug` مسجَّلة، فالأسبوع كان مفتوحاً طوال الوقت. **الحاجز الحقيقى كان مفتاح حساب الخدمة** (`DRIVER_FCM_PROJECT_ID` + `DRIVER_FCM_CREDENTIALS_PATH` فى `.env` الباك اند؛ المشروع `meniura-driver`، والمفتاح فى `storage/app/private/fcm-meniura.json` — **غير مُلتزَم، ولا يجوز**).

| السيناريو | النتيجة المقيسة |
|---|---|
| الشاشة مطفية ومقفولة | ✅ الإشعار على قناة `offers`، والجهاز صحى (`Dozing` ⇒ `Awake`) |
| **Doze عميق مفروض** (`dumpsys deviceidle force-idle`) + على البطارية | ✅ **النظام كان قتل العملية والدفعة أحيتها** (PID ٦٥٧٢ ⇒ ١١٦١٧) ثم `notify(4501, channel=offers)` والتنبيه الصوتى اشتغل. الشاشة **لا** تُضاء تحت Doze العميق — الصوت والاهتزاز هما ما يوقظ السائق، وهو سقوط الأسبوع المُجمَّد نفسه |
| توزيع إنتاجى حقيقى (طلب ٣٩٥، الشاشة مقفولة) | ✅ العرض وصل؛ **الاستطلاع** التقطه أولاً (`raising offer 8`) ⇒ إثبات مباشر لذراعَى «صفر إشعار ضائع» |
| دفعة لعرض مُقَرّ (acked) | ✅ **الخادم رفضها بنفسه** (`push_count` بقى ٠) — الطبقة الأولى من منع الازدواج |
| **إشعار متأخر لعرض ميت** | ✅ يُغلق نفسه ويُعيد السائق لمكانه (كان يحبسه — انظر المزالق) |
| `force-stop` يدوى من المستخدم | ⚪ **لا يرنّ — وهذا سلوك أندرويد لا عطل عندنا** (`FLAG_STOPPED` يمنع FCM حتى يفتح المستخدم التطبيق مرّة). لا تُضِعْ وقتاً فى «إصلاحه» |

**الباك اند:** `POST/DELETE /driver/device-token` (`driver.auth:account`) · **التسجيل يسرق التوكن من أى سائق آخر يحمله** فى نفس المعاملة — التوكن ملك **الجوال** لا الحساب، ومن يحذف التطبيق لا يستدعى `logout` أبداً · `logout` يمسحه خادمياً · **`SendDriverOfferPush`** صار المكان الوحيد الذى يصوغ الحمولة، ويُطلَق **فوراً** من `DriverOfferPushScheduler` — قبل ذلك كانت `ResendDriverOffer` (بعد ١٥ث) هى الوحيدة التى تنادى FCM، أى أن **الموجة الأولى من كل عرض لم تكن تُدفَع أبداً**.

**العقد:** `Offer.id` صار إلزامياً — الاستطلاع كان يرى `expires_at`/`wave` فقط بينما الدفع يحمل `offer_id` ⇒ **لا مفتاح مشترك بين الذراعين**، و«صفر إشعار ضائع» كانت ستتحوّل لـ«رنّتين لكل طلب». ٢٨ مساراً · ٢٩ عملية · ١٧ مخطّطاً.

**التطبيق:** وحدة **`:core:push`** جديدة (هى وحدها التى تعرف Firebase Messaging؛ `PushHandler` هو السطح، و`:app` يوفّر التنفيذ عبر `@Binds` فلا دورة) · **`OfferGate`** (مفتاحه `offerId`، ذرّى، محدود الذاكرة) و**`OfferCoordinator`** (الحالة المشتركة، تُكتب حتى بلا ViewModel حىّ) · `DeviceTokenRegistrar` · **`POST_NOTIFICATIONS` يُطلَب الآن ولافتة صحة الإشعارات موصَّلة**.

**🔴 خمسة أعطال أُصلحت، ثلاثة منها لا يكشفها إلا جهاز:**
1. **الإذن لم يكن يُطلَب أصلاً** إلا كآخر رُتبة فى سُلّم الموقع ⇒ على S25 كان `granted=false` و**`numEnqueuedByApp=37 / numPostedByApp=0`**: التطبيق رنّ ٣٧ مرة والسائق لم يرَ شيئاً، بلا أى أثر فى الواجهة. و`NotificationHealth` (مطلب مُجمَّد) **كانت مكتوبة بالكامل ولا يستدعيها أحد**.
2. **`Instant.parse` يرفض الإزاحة الرقمية.** الخادم يرسل `...+03:00` (توقيت الرياض)، و`ISO_INSTANT` لا تقبل الإزاحة قبل JDK 12، والمشروع بلا desugaring ⇒ السلوك يقرّره **كل جهاز**. على S25 تعمل، وعلى جوال سائق رخيص **كل دفعة تُسقَط صامتاً**. الحلّ فى المكانين (`OfferPushPayload.parseInstant` و`parseOfferInstant`).
3. **إشعار متأخر كان يفتح شاشة ميتة**: `not_your_order` كان يُعرَض كـ`LoadFailed` (لافتة حمراء وسط شاشة فارغة بلا مخرج ولا إعادة محاولة). الآن الرموز النهائية تُحَلّ على مسار **التحميل** لا مسار الفعل فقط.
4. **`wave` كان يقدر يرفض عرضاً حيّاً** رغم أنه `nullable` فى العقد ولا تعرضه شاشة.
5. **أيقونتا الإشعار كانتا `android.R.drawable.*`** — أيقونة النظام تُرسَم كبقعة رمادية. صارتا ظلّية بيضاء لكل وحدة (`ic_notification_offer` / `ic_notification_location`)، ومؤكَّد على الجهاز أن المورد صار من حزمتنا.

**⏳ ما ينتظر صاحب المشروع:** `DRIVER_FCM_PROJECT_ID`/`DRIVER_FCM_CREDENTIALS_PATH` على **خادم الإنتاج** · و`php artisan migrate` ثم **`route:cache` إلزامى** (مساران جديدان) · وقرار الدفع.

### ✅ أسبوع ٥ — شاشة الرحلة + رمز استلام العميل (قرار ٤٨)

**الباك اند:** المسارات الأربعة `picked-up`/`delivered`/`issue`/`breadcrumbs` بـ`Idempotency-Key`، كل تغيير حالة عبر `transitionTo()` حصراً · `delivered` هى المكان الذى يُقيَّد فيه الأجر والنقد فى الدفتر (`recordDeliveryEarning`/`recordCashCollected` كانتا مبنيّتين ولا يستدعيهما أحد) · **منع التقييد المزدوج ضمانة قاعدة بيانات** (فهرس فريد `(order_id, type)` من أسبوع ٤) · `OccurredAt` سطح واحد للقصّ بين (الآن−٦س) والآن يخزّن القيمتين · `SweepPickupLease` يعيد الطلب للطابور عبر `DriverAssigner` وحده · `OrderDeliveryEvent` + `TripBreadcrumb` + `PurgeOldBreadcrumbs`.

**رمز استلام العميل (قرار ٤٨ — صاحب المشروع، بديل مجانى لـOTP):** `orders.delivery_code` أربعة أرقام تُولَّد **لطلبات التوصيل فقط** فى كل مسارات الإنشاء عبر `DeliveryCodeGenerator` · العميل يقرأه من صفحة طلبه أو يقوله له الكاشير فى نفس المكالمة · `delivered` يتحقق بـ`hash_equals` تحت قفل، بحدّ **٥ محاولات** ثم `too_many_attempts` · **إعفاء الفرع** (لا السائق) بسبب إلزامى ومَن اعتمده · طلب مقسَّم يأخذ **رمزاً جديداً** لا يرث رمز أبيه.

**🔴 ثلاثة أسطح ممنوع أن يظهر فيها الرمز، ولكلٍّ اختبار:** ردّ `/driver/*` · **الفاتورة المطبوعة** (ملزوقة على الكيس الذى يحمله السائق) · **تذكرة/طابور المطبخ**. الأخيرة كانت ستتسرّب فعلاً: `KdsController::ticket()` و`pendingKitchenPrints()` يعيدان استخدام `PosOrderController::formatOrder()` نفسها التى تحمل الرمز للكاشير.

**`requires_delivery_code` بوليان على `DriverOrderAssigned`:** بوليان لا يسرّب شيئاً (أربعة أرقام تبقى غير قابلة للتخمين) ويشترى الشىء الوحيد المهم — أن يسأل التطبيق عن الرمز **والسائق ما زال أمام العميل**، بدل أن يكتشف الاشتراط من `delivered` رفضه الخادم مرّة.

**التطبيق:** `:feature:trip` الشاشات ١٠/١١/١٢ · «حصّل: X» بخط كبير ومبلغ قابل للتعديل بسبب إلزامى · تأكيد بضغطة مطوّلة · ملاحة خارجية · **جوال العميل وعنوانه يُمحيان من الحالة لحظة نجاح `delivered`** (اختبار يثبتها بخادم يُصرّ على إعادة إرسالهما).

**🐞 تسعة أعطال حقيقية أُصلحت — ستة منها لا يكشفها إلا جهاز حقيقى:**
1. **زرّ «استلمت الطلب» فى قائمة الطلبات كان `onClick = {}`** — أسبوع ٣ بنى الشكل وأجّل المُعالِج لأسبوع ٥، فكان سائق ماسك طلب يرى زرّاً كبيراً لا يفعل شيئاً، ولا طريق لشاشة الرحلة إلا قبول عرض جديد.
2. **لا `BackHandler` فى أى مكان** — ضغطة رجوع وسط التوصيل **تُغلق التطبيق**. أُضيف للرحلة ومركز الإشعارات؛ وشاشة العرض تبقى بلا واحد عمداً (تُجاب ولا تُرفَض).
3. **`occurred_at_device` كان يُخزَّن UTC فى عمود يُقرأ بتوقيت الرياض** ⇒ وقت التسليم غلط بثلاث ساعات للأبد.
4. **`DriverLedgerService::todayRangeFor` كان يحوّل حدّ اليوم لـUTC** ⇒ **`earned_today` تُرجِع صفراً دائماً** لكل سائق (بقّ أسبوع ٤).
5. الرمز كان سيتسرّب لشاشة وطابعة المطبخ (أعلاه).
6. حقل الرمز كان يظهر **بعد** رفض الخادم لا قبله.
7. **🔴 زرّ «تأكيد التسليم» لم يكن يعمل إطلاقاً.** كُتب كـ`Button` مع `combinedClickable` خارجى، و`Button` يحمل `clickable` داخلياً يبتلع الإيماءة قبل أن تصل للخارجى ⇒ الضغطة المطوّلة لا تصل أبداً، على **الزرّ الوحيد الذى يُنهى التوصيل**. ولا اختبار وحدة يمكنه رؤيته: الاختبارات تنادى `confirmDelivery` مباشرةً ولا تؤدّى الإيماءة. (ثم أُلغيت الضغطة المطوّلة أصلاً — انظر أدناه.)
8. **الكيبورد كان يغطّى خانات الرمز وزرّ التأكيد.** `enableEdgeToEdge()` يُبطل `adjustResize` فى الـmanifest، فالـIME صار inset علينا نحن، ولم يكن هناك `imePadding` فى أى مكان. ⚠️ ومكانه الصحيح هو **الصندوق الخارجى** الذى يثبّت الورقة فى القاع، لا العمود داخلها — المحاولة الأولى وضعته داخلياً فحرّكت المحتوى داخل ورقة ما زالت فى المكان الخطأ.
9. **زرّ «استلمت الطلب» يُعطَّل بعد الاستلام** (صحيح) — وكان هو المدخل الوحيد لشاشة الرحلة، فسائق **فى منتصف التوصيل** لا طريق له لـ«سلّمت». البطاقة كلها صارت تفتح الرحلة.

**↩️ قرار صاحب المشروع (أثناء الاختبار الميدانى): لا ضغطة مطوّلة — ضغطة عادية.** «لا نريد أى شىء صعب على السائق». وهو صحيح لسبب إضافى: **الرمز نفسه صار هو التأكيد** — لمسة عابرة تصل خادماً يرفضها بلا أربعة أرقام لا يملكها إلا العميل. الإيماءة كانت تحرس باباً يقفله الرمز.

**الأرقام:** الباك اند **١٨٧ اختبار سائق** (كانت ١٦٢) والمجموعة الكاملة **٤٠٥٩** صفر فشل · التطبيق: النكهتان تُبنيان و`verify.sh` أخضر · الفرونت: `tsc`+`eslint` نظيفان.

**⚠️ لم يُجرَّب باليد:** دورة **التسليم بالرمز** على S25 (انقفلت الشاشة). المُجرَّب فعلياً: «استلمت» على بيانات حقيقية (`ready → out_for_delivery` والحدث مختوم بالوقتين)، وأن الرمز يصل **فعلاً** لصفحة العميل على السلك (`delivery_code = "4821"`) بينما هو غائب تماماً من ردّ السائق.

**⚠️ بند مفتوح:** `openapi/pos.v1.yaml` تغيّر (أُعلن `delivery_code` لسطح الكاشير) ⇒ **مرآة `pos.v1.json` فى ريبو POS الأندرويد بائتة**. **انحراف توثيقى لا كسر** (استهلاك جزء من المخطط مسموح، وDTOs الـPOS لا تحمل الحقل). لم تُحدَّث لأن صاحب المشروع حدّد ريبو POS **للقراءة فقط** فى هذه الجلسة.

**⏳ معلّق ولم يُنفَّذ:**
- **مستقبِل FCM على الجهاز + الإشعار فى الخلفية.** `:core:notifications` مبنية وجاهزة (قناة بأهمية قصوى + تجاوز DND + شاشة كاملة مع سقوط نظيف على أندرويد ١٤+) لكن **لا شىء يُغذّيها بعد**: العرض اليوم يصل عبر **الاستطلاع كل ١٥ث** وهو ذراع أصيلة فى معمارية «صفر إشعار ضائع» لا بديل مؤقت. ⚠️ **يعنى: التطبيق مغلقاً لا يرنّ بعد.**
- **Firebase بلا بصمة SHA-1** ⇒ لا Phone Auth ولا FCM يمكن إثباتهما على جهاز.
- **انحراف ساعة الجهاز فى العدّاد** (قرار مؤجَّل بوعى): العدّاد يقارن ساعة الجوال بلحظة الخادم المطلقة، فجوال ساعته **متقدّمة** تنتهى شاشته لحظة ظهورها والسائق لا يرى العرض أصلاً. الخادم محمى (يرفض `offer_expired`) لكن السائق يخسر الطلب صامتاً. الحل حين يلزم: معايرة الفرق من ترويسة `Date` فى أول رد.
- الترجمات الثلاث (ur/bn/hi) بانتظار مراجع أصلى — **ومنها أكواد العرض الجديدة** (`already_claimed`, `offer_expired`, `too_many_active_orders`, `cash_limit_exceeded`).
- **الاختبار الميدانى بالجيب ساعة** — استُبدل عملياً بـ**Doze مفروض** (يُثبت نفس الشىء فى دقيقتين بدل ساعة، ويفرض أسوأ حالة بدل انتظار وقوعها). يبقى تأكيداً نهائياً مع سائق حقيقى قبل البيتا.
- ⚠️ على الإنتاج: `php artisan migrate` ثم **`route:cache` إلزامى** (٥ مسارات جديدة).

**المُنجَز فى جلسة 2026-09-21 (الرابعة) — الأسبوع ٤: محرك التوزيع والعرض والقفل:**

*الباك اند:*
- **`driver_offers`** + `DriverOfferOutcome` + `DriverOffer`. 🔴 **«عرض معلّق واحد لكل سائق» مفروض فى قاعدة البيانات لا فى فحص PHP**: عمود مولَّد `pending_driver_key` (= معرّف السائق ما دام العرض `pending`) عليه فهرس فريد، وMySQL يتجاهل NULL فيه فتبقى العروض المُجابة بلا حدّ.
- **`App\Services\Delivery\DriverAssigner`** — الكاتب الوحيد لـ`orders.delivery_driver_id`. الإسناد `UPDATE … WHERE delivery_driver_id IS NULL` **لا read-then-write**، فلا يعتمد على مستوى العزل (نفس الفخّ الذى أنتج حجزاً مزدوجاً فى محرك طاولات الأماكن). **وحُوّل البابان القديمان إليه** (`ViewOrder` و`CompanyOrderActionController`) — ومكسب جانبى: التعيين اليدوى صار يُنهى العرض الذى يعدّ عليه سائق آخر، وكان يتركه يعمل.
- **`orders.driver_claimed_at`** (يُكتب فى نفس الجملة الذرّية) و**`orders.driver_dispatch_opened_at`**. 🔴 التساقط على `self_claim` **حقيقة مخزَّنة لا مشتقّة**: «لا يوجد عرض حىّ الآن» صحيحة أيضاً فى الجزء من الثانية بين موجتَى العرض، واشتقاقها كان سيفتح الطلب لكل سائقى الشركة فى تلك النافذة بالذات.
- **الخمسة**: `POST driver/orders/{id}/{ack,accept,decline,claim,release}` بـ`Idempotency-Key` إلزامى، و`IdempotencyKey::actorFor()` صار يعرف السائق (كثيرون خلف NAT شركة اتصالات واحدة).
- **الوظائف الأربع** (`DispatchOrderToDrivers` المؤجَّلة · `ExpireDriverOffer` · `ResendDriverOffer` · `NotifyRestaurantNoDriver`) — كلها تعيد التحقق وقت التنفيذ، والأخيرة تستخدم قناة إشعار الموظفين الموجودة لا قناة ثانية.
- **`App\Services\Fcm\FcmService`** — FCM v1 بحساب خدمة، رسالة بيانات عالية الأولوية، **بلا أى تبعية جديدة** (JWT بـ`openssl_sign`)، ويتدهور بأمان (يُرجِع false ولا يرمى) كما يفعل `DriverLocationService` مع Redis.

*التطبيق:*
- **`:feature:trip`** — شاشة العرض (شاشة ٩): عدّاد **إلى `offer.expires_at` الخادمى**، ACK فور الظهور بحارس لمرّة واحدة، قبول/رفض بأهداف `TouchTarget.primaryPhysical` (٦٤dp حقيقية مهما صغّر السائق الواجهة)، وبلا أى حقل يستطيع حمل اسم العميل أو عنوانه (ضمان زمن-تصريف لا انضباط).
- **`:core:notifications`** — قناة العروض بأهمية قصوى + تجاوز DND + صوت واهتزاز متكرران، و**سقوط نظيف** حين تُرفض `USE_FULL_SCREEN_INTENT` على أندرويد ١٤+ (نفس الإشعار يبقى heads-up عالى الأولوية: العرض يصل فى الحالتين).
- **`PendingOfferViewModel` فى `:app`** — الاستطلاع كل ١٥ث، وشاشة العرض تعلو كل شىء (حتى مركز الإشعارات) وتُعيد السائق لمكانه بالضبط.

**المُنجَز فى جلسة 2026-09-21 (الثالثة) — الأسبوع ٣ + الدفتر + إعادة تصميم:**

*الباك اند:*
- **إعدادات «التوصيل بالسائقين» لكل فرع** (١١ عموداً بالافتراضات المجمَّدة) + قسمها فى `BranchForm` مبوَّباً بـ`PlanFeature::DeliveryManagement`، وكل وصف مكتوب بلغة صاحب المطعم. وعمود `orders.driver_fee` (لقطة الأجر، nullable بلا backfill).
- **`App\Services\Delivery\DispatchPolicy`** — السطح الوحيد الذى يقرأ وضع الإسناد والتوقيت. `grep` على `driver_assignment_mode`/`driver_dispatch_timing` لا يُظهر شيئاً خارجه.
- **`GET driver/orders/{available,mine,{id}}`** بشكلَى العقد، والفرق بينهما **بنيوى** (مفاتيح العميل والعنوان غائبة من المصفوفة، لا `null`).
- **الدفتر المالى كاملاً** (`driver_ledger_entries` + `driver_settlements` + `DriverLedgerService` + `GET driver/ledger{,/settlements}`) — دفتر لكل (سائق × مطعم)، بلا جمع ولا تحويل عملة. وبه أُغلق stub السقف النقدى: السائق المتجاوز يخرج من ترشيح **الطلبات النقدية فقط**.

*التطبيق:*
- **`:feature:orders`** — تبويبا «طلباتى»/«المتاحة» + بطاقة الطلب + عدّاد «جاهز خلال ~N د».
- **`:feature:wallet`** و**`:feature:account`** و**`:feature:notifications`**، وشريط تنقّل من أربع وجهات. **حُذفت `:feature:home`** — محتواها صار داخل «حسابى».
- **إعادة تصميم كاملة بعد رفض صاحب المشروع للشكل**: سطح أبيض بحدّ رفيع وظل بدل الصناديق الرمادية · علامة المطعم · شارات ملوّنة للحالة · الأجر رقماً واحداً كبيراً · هيدر مشترك بجرس فى كل وجهة.
- **تصغير الواجهة درجة** + **`UiScaleStore`** بأربع درجات تحرّك الكثافة لا الخط وحده، مع `TouchTarget.primaryPhysical` كى يبقى زرّ الرحلة ٦٤dp زجاجاً حقيقياً مهما صغّر السائق.

- `CLAUDE.md` كامل — الـ44 قراراً محسومة + بروتوكول الجلسة + نظام التصميم + الأقسام السبعة المضافة.
- `git init` + `.gitignore` + الريبو مربوط ومدفوع.
- **بنية Gradle منسوخة من POS ومُعاد تسميتها** `pos` → `driver`: wrapper · `gradle.properties` · `settings.gradle.kts` · `libs.versions.toml` · `build-logic` (المعرّفات صارت `driver.android.*`) · `Flavor.kt` (نكهتا meniura/taaj بلاحقة applicationId لكل منهما).
- **وحدة `app`** تبنى وتعمل: `DriverApplication` (Hilt) · `MainActivity` (شاشة تحقّق مؤقتة) · أنواع البناء الثلاثة (debug/staging/release) · حارس التوقيع · `versionCode = 10000` بالصيغة المجمَّدة · ألوان العلامة لكل نكهة · اسم التطبيق بالعربى والإنجليزى.
- **مُتحقَّق على الجهاز:** النكهتان تُبنيان · التثبيت والإقلاع بلا انهيار · `منيورا للسائقين` تُعرض عند فرض العربية · لقطة فى `_screenshots/`.

- **`:core:designsystem` مكتملة:** `Tokens.kt` (بعتبات لمس الجوال: كل إجراء رحلة 64dp) · `Type.kt` (IBM Plex + مقياس أكبر درجة من POS للقراءة بيد واحدة) · `AccentPalette.kt` (حارس تباين 4.5:1) · **`Theme.kt` بالانحرافات الأربعة موثّقة فى رأس الملف** · خطوط IBM Plex الأربعة · ألوان العلامة لكل نكهة داخل الوحدة (لا فى `app`) · `DriverTheme` موصولة بـ`MainActivity`.
- **مُتحقَّق بقياس البكسل** (لا بالعين): الفاتح `#F1EFEA` ≈ المحسوب `#F3F0EB` ✅ · الداكن `#0D0B08` ليس أسود نقياً ويحتفظ بدفء العلامة (انظر المزالق).

- **`:core:common` + `:core:datastore` مكتملتان ومسجَّلتان فى البناء** (جلسة 2026-09-21):
  - `SupportedLocales` (نقى، بلا AndroidX): الخمس لغات + مجموعة RTL (`ar`,`ur`) + `buildLatinLocale` بامتداد `nu-latn`.
  - `TokenExpiry` (نقى): **«منتهٍ» يُحسب من الوقت المخزَّن مقابل الساعة، لا علَم يُكتب وقت الدخول** (علَم كهذا يكذب فى اليوم التالى). و`expires_at` **غائب ⇒ يُعدّ حياً لا منتهياً** — الخادم هو صاحب القرار، وردّ لم يحمل انتهاءً يجب ألا يُخرج السائق من جلسة تعمل (الميت يعود 401 ويُمحى حينها). والرمز المنتهى **لا يُبلَّغ كـ«يحتاج تجديداً»** بل كتسجيل دخول.
  - `AppLocaleStore`: `language` **nullable** ليميّز «لم يُختَر» عن «اختار العربية» — بدون ذلك تظهر شاشة اللغة كل مرة أو لا تظهر أبداً (قرار ٩).
  - `LocaleManager`: **بمتجر واحد لا اثنين** (POS يوفّق بين DataStore وكاش متزامن؛ هنا `AppLocaleStore` وحده هو الدائم وهو ما يقرؤه `attachBaseContext` قبل وجود Hilt). و`restore()` **لا يكتب شيئاً إن لم يُختَر** وإلا بدا كاختيار صريح للعربية فلا تعود الشاشة تظهر.
  - `TokenStore` على `EncryptedSharedPreferences` + استرجاع من keystore معطوب (حذف الملف وإعادة البناء — تسجيل دخول واحد أرخص من تطبيق لا يُفتح). **يخزّن أقلّ بكثير من نظير POS عمداً:** التوكن مربوط بـ`driver_id` والسائق يعمل لعدة مطاعم ⇒ **«الشركة الحالية» ليست خاصية للجلسة أصلاً**، وتخزينها كان سيخترع حالة لا يملكها النموذج والدفتر (لكل سائق×مطعم) هو من يدفع الثمن. و`driverId` **يُخزَّن** لأن قناة Reverb هى `private-driver.{driverId}` ويلزم الاشتراك قبل تحميل أى شاشة. و`clear()` **يُبقى الهاتف** (ليس سرّاً، والسرّ هو الرمز).
- **`openapi/driver.v1.yaml`** — ٢٦ مساراً · ٢٦ عملية · ١٦ مخطّطاً · ٩٤ مرجعاً كلها تُحَلّ (مُتحقَّق بالتحليل الفعلى). **خصوصية العميل مفروضة بالبنية:** `DriverOrderOffered` لا يحتوى حقلَى العميل والعنوان أصلاً (ليسا `null`)، و`DriverOrderAssigned` يضيفهما بـ`allOf` ⇒ يستحيل إرسال جوال العميل قبل القبول بالنسيان. و`Idempotency-Key` **إلزامى** على كل أمر رحلة، وأكواد الأخطاء الـ٢٦ المجمَّدة `enum` فى المخطّط. و**`AvailabilityContext`** على `orders/available` ينفّذ القرار ٤٧ (الخادم يسمّى سبب عدم وجود طلبات من سبعة أسباب — لأنه وحده يعرف نصف القطر وقواعد الترشيح).
- **`.claude/skills/` السبع** مكيَّفة للسائق لا منسوخة (`driver-ui-standards` بـ٦٤dp ومعاينة هاتف عمودى، والـendpoint بـ`DriverToken`+`Idempotency-Key`+الترجمة بالكود، والشاشة بخمس لغات).
- **`scripts/verify.sh`** (يضبط `JAVA_HOME` بنفسه، يبنى النكهتين، `hiltJavaCompile`، ويُشرِط اختبارات `:core:database`/`:core:network` بوجود المهمة فعلاً، **ويطبع صراحةً ما لا يستطيع إثباته**: الجهاز الحقيقى والموقع الخلفى وFCM وOTP) و**`PLAN.md`** (مرحلة ١ بتبعياتها وتعريف «تمّ» ملاحَظ لكل خطوة + تبعية الباك اند بارزة).

- **`:core:network` مكتملة** (جلسة 2026-09-21، غير مُلتزَمة):
  - `NetworkModule`: Retrofit على `API_BASE_URL + "/api/v1/"` (لا `/pos/`)، وسلسلة اعتراض من أربعة: `DnsRetry` (الأبعد، لأجل أجهزة OEM التى تجمّد التطبيق) ← `DriverAuth` ← `Locale` ← `AppVersion`.
  - **`coerceInputValues` مُطفأ عمداً** (بخلاف POS): مع التفعيل يُعاد كتابة `null` صريح إلى الافتراض — وحقول هذا السطح **مال**، فـ`cash_to_collect` كان سيصير 0.00 ويحصّل السائق لا شىء. كل حقل يسمح العقد بأن يكون null هو nullable فى الـDTO، ويحرسه اختبار المرآة.
  - **`Idempotency-Key` معامل إلزامى فى توقيع كل أمر رحلة** فى `OrderApi` (لا interceptor يقرأ tag اختيارياً كما فى POS): المترجم يفرض ما يفرضه العقد، فيستحيل نسيانه — وبدونه تُقيَّد رحلة أُعيد إرسالها مرّتين فى الدفتر.
  - **`PublicEndpoint` وسم** لا مطابقة مسار: `app-version`/`request-otp`/`verify-otp`/`login` لا تُوقَّع، وإعادة تسمية مسار لا يمكن أن تُسرّب توكناً ولا أن توقفه صامتاً.
  - **`DriverOrderDto` نوع واحد للشكلين** (offered/assigned): ضمانة الخصوصية بنيوية **على الخادم**، وتصل هنا كـ**nullability** فيستحيل عرض جوال عميل لم يُقبَل طلبه بعد. ويحرسها اختبار صريح على العقد نفسه.
  - `DriverErrorCode` الـ٢٦ المجمَّدة + `DriverIssueCode` الخمسة، **كلاهما مُقارَن بالعقد فى اختبار** — والكود المجهول يُرجِع null لا استثناءً (تطبيق قديم أمام خادم أحدث).
  - مرآة العقد `contract/driver.v1.json` (٢٦ مساراً · ١٦ مخطّطاً) + `BackendContractMirrorTest` يفهم `allOf` و`components.responses`.
- **رقم الإصدار صار مصدراً واحداً** `build-logic/…/Version.kt` (`DriverVersion`): يقرؤه `app` لـ`versionCode/versionName` **و**كل وحدة عبر `BuildConfig.APP_VERSION_CODE`. كان الرقم فى مكانين (`10000` فى app و`"0.1.0-meniura"` فى `Flavor.kt`) — والمُحدِّث يقارن ما يبلّغه العميل بما يختمه المثبّت، فاختلافهما يقفل سائقاً محدَّثاً أو يترك قديماً يعمل، **صامتاً فى الحالتين**.

- **شاشة اختيار اللغة مكتملة** (`:feature:onboarding`، الشاشة ١، غير مُلتزَمة):
  - **أسماء اللغات ليست موارد نصّية ولا يجوز أن تصير كذلك** — تُعرض كل لغة باسمها فى لغتها فى كل الحالات. الشاشة موجودة أصلاً لسائق لا يقرأ اللغة المعروضة الآن؛ ترجمة الأسماء كانت ستعطى من يقرأ البنغالية وحدها خمس كلمات لا يفهمها.
  - **المعاينة الحيّة بلا التزام:** الضغط على صف **لا يكتب شيئاً**؛ الشاشة تحلّ **نصوصها هى** فى لغة الصف المختار عبر `createConfigurationContext`. الكتابة على الضغط كانت تجعل اختياراً نصف-منجز دائماً (يقفل التطبيق ⇒ لا تظهر الشاشة ثانيةً)، و`AppCompatDelegate.setApplicationLocales` **لا يُعيد رسم `ComponentActivity` عادية أصلاً** — فالادّعاء بأنها «تتحوّل فوراً» كان سيبقى مكتوباً وغير صحيح.
  - **كل صف يُرسَم باتجاهه هو** لا باتجاه التطبيق: «اردو» تبقى يميناً والتطبيق بالإنجليزية.
  - `LanguageOption.all` **مبنية من `SupportedLocales`** فيستحيل أن تدعم المنصة لغة لا يعرضها المنتقى.
  - **لا `NavHost` بعد** — رسم تنقّل بوجهة حقيقية واحدة يُعاد كتابته يوم تصل شاشة الدخول، ويُخفى من يقرّر ما الذى يعمل أولاً. القرار صريح فى `MainActivity`.
  - اختبارات: اكتمال المنتقى، الأسماء الأصلية، الاتجاه، **ووجود المفاتيح الثلاثة فى الخمس لغات** (مُتحقَّق أنه يفشل عند حذف مفتاح واحد).

- **شاشة الدخول مكتملة** (`:feature:auth`، الشاشة ٢، غير مُلتزَمة) — بملاحظات صاحب المشروع الأربع:
  - **كود الدولة يساراً والرقم يُكتب من اليسار فى كل اللغات:** الصفّ كله مثبَّت `LayoutDirection.Ltr`. رقم الجوال ليس نثراً — يُطلَب من اليسار ومطبوع كذلك على الورقة التى أعطاها المطعم للسائق؛ ولو انعكس فى العربية لصار `+966` يمين الأرقام و`+` يتنقّل أثناء الكتابة. **العنوان فوق الحقل يبقى باتجاه الواجهة** (هو نثر، والقيمة ليست).
  - **الكود يُكتشَف من الشريحة** (`simCountryIso` ← `networkCountryIso` ← الـlocale) عبر libphonenumber. **ليس من لغة الجهاز:** سائق بنغالى يعمل فى الرياض شريحته سعودية وتطبيقه بالبنغالية، ولغة الجهاز كانت ستخمّن بنغلاديش وتبنى رقماً خاطئاً صامتاً. ويبقى منتقى يمكنه تجاوزه.
  - **زر إظهار الرمز:** الرمز أربعة أرقام تُكتب بيد واحدة فى سيارة، و«رمز خاطئ» من الخادم لا يمكن تمييزه عن «نسيت الرمز» وأنت ترى أربع نقاط. يبدأ مخفياً (قد يناول السائق جواله أو يجلس بجوار أحد).
  - **رقم الإصدار أسفل الشاشة** (`1.0.0 (10000)`، معزول الاتجاه): أول ما يُسأل عنه عند أى مشكلة، وهو ما يقارنه المُحدِّث.
  - **التحقق من الرقم محلياً قبل الإرسال** — لا يحرق محاولة من الخمس/دقيقة التى يسمح بها limiter الدخول.
- **`GET /driver/branding` أُضيف للعقد** (المسارات ٢٧، المخطّطات ١٧): **الشعار واسم المنصة يأتيان من لوحة الأدمن لا من الـAPK** — شعار مُجمَّع فى التطبيق يعنى أن تغيير الهوية يحتاج نسخة جديدة على جوال كل سائق. عام لأنه يُقرأ **قبل** الدخول. **الفشل صامت تماماً**: لا لافتة ولا تعطيل للدخول (السائق جاء ليدخل، غالباً على شبكة ضعيفة)، ويسقط على اسم النكهة وبلا شعار.
  - ⚠️ **لم يُبنَ فى الباك اند بعد** — لذا الشاشة تعرض الاسم الاحتياطى اليوم. نظيره الموجود: `PosAuthController::branding`.
- **`:core:ui` وُلدت هنا** لأن هذه أول شاشة تُنادى الشبكة: `BidiText` (عزل اتجاه القيم) + `DriverErrorBanner` + **خريطة الأكواد الـ٢٦ إلى نصّ فى الخمس لغات**.
  - **«لا اتصال» و«رفض الخادم» شكلان مختلفان عمداً**: الأول يصلح نفسه بنفسه والثانى يحتاج اتصالاً بالمطعم؛ لونهما الواحد يُعلّم السائق تجاهل الأهم. وزر «حاول» يظهر فقط حين تُجدى المحاولة.

**✅ المرحلة ١ الأسبوع ١ — الباك اند: مُنجَزة** (2026-09-21، commit `bf9ebfc7` فى ريبو الباك اند، **غير مدفوع**):
- **الجداول الأربعة**: `fleets` · `drivers` · `driver_restaurant_links` · `driver_tokens`. **بلا `country_code` على `drivers`** (قرار ٤٥ محترم).
- **`DriverToken`** مطابق لنمط `PosStaffToken` بالحرف (sha256، النصّ الصريح لا يُخزَّن، ليست Sanctum)، **٣٠ يوماً**، جلسة جهاز واحدة.
- **`DriverEligibility` سطح واحد** يجيب «هل يعمل هذا السائق الآن ولأى مطاعم؟» — يقرؤه الـmiddleware **و**`login` معاً. وفقدان مطعم واحد (اشتراك منتهٍ/تخفيض/تعطيل) **لا** يقفل السائق عن الباقى.
- **`driver.auth` بوضعين**: السلسلة الكاملة لـ`/driver/me`، و`account` فقط لـ`set-password`/`redeem-invite`/`logout` — لأن السائق لحظة إثبات جواله **بلا أى رابط**، و`redeem-invite` هى الطريق الوحيد لأول رابط؛ تبويبها بوجود رابط = قفل مغلق.
- **المسارات الثمانية**: `branding` (عام) + `auth/{request-otp,verify-otp,set-password,login,redeem-invite,logout}` + `me`.
- **Firebase خلف واجهة `FirebasePhoneVerifier`**: الحقيقى مثبَّت على RS256 ويفحص `aud`/`iss`/`exp`/`phone_number`؛ والبديل يُرجِع true **فقط خارج الإنتاج وبلا مشروع مضبوط**، وفى الإنتاج **يفشل مقفولاً** ويسجّل `critical`.
- **شاشة دعوة السائق** فى لوحة الشركة: اسم + جوال ⇒ كود ٦ أحرف بلا `0/O` ولا `1/I/L` + رابط واتساب. وإعادة دعوة سائق **نشط** لا تُنزّله لـ«مدعوّ».
- **`PlanFeature::DeliveryManagement`** بالـNew-Feature Checklist كاملاً (Professional + Enterprise).
- **٦٤ اختباراً جديداً**؛ المجموعة الكاملة **3932 أخضر** (كانت 3868)، صفر انحدار.
- ⚠️ **النشر يحتاج `migrate` ثم `route:cache`** — المسارات الثمانية تعطى 404 بدونه.
- **`RestaurantLink.cash_limit` يُرسَل `null` دائماً** — لا عمود له بعد (الدفتر أسبوع ٦)، وهى قيمة مشروعة فى العقد («صفر أو null = بلا سقف»). مَن يبنى السقف النقدى يضيف العمود ويوصّل الحقل.

**🔴 انحرافان عن العقد وقعا فعلاً وأُصلحا فى نفس الجلسة** (مراجعة قائد الجلسة لشغل الوكلاء — لا تسلّم بتقرير وكيل):
- `login` كان يُصدر توكناً لسائق **موقوف** أو شركته منتهية، والعقد يوثّق خمسة أكواد 403 على **هذا** المسار بالذات. شاشة الدخول هى المكان الوحيد الذى يتصرّف فيه السائق إزاء «حسابك موقوف»؛ إعطاؤه توكناً وإفشال النداء التالى يعرض خطأ عاماً على شاشة غادرها بالفعل.
- رابط شهادات جوجل كان يحمل `metadata/x509` **مكرراً** ⇒ كل تحقّق من توكن Firebase كان سيفشل، ولا يظهر العطل إلا يوم يُضبط المشروع.
- وثالثة أصغر: مقارنة رقم الإصدار كانت نصّاً مقابل عدد صحيح ⇒ «تغيّر البناء» صحيحة فى **كل** طلب فتتحوّل كبحة «مرّة كل دقيقة» إلى كتابة قاعدة بيانات لكل نداء.

**✅ الأسبوع ٢ مكتمل أيضاً (نفس الجلسة) — «متاح» + الموقع:**
- **الباك اند** (`54d81fd8`): `PATCH driver/availability` + `POST driver/location` + `DriverLocationService` (Redis GEO، مجموعة لكل شركة) + `DriverAvailabilityService` + أمر `driver:sweep-stale` كل دقيقة. **MySQL تُكتب مرّة/دقيقة فقط** (`last_seen_at`)، والباقى فى Redis. **Redis واقع ⇒ تدهور رشيق** لا 500، والكنس يقرأ MySQL لا Redis عمداً كى يبقى صحيحاً أثناء العطل.
- **`:core:location`**: الخدمة الأمامية (نوع `location`)، الإيقاعات المجمَّدة (٧ث متحرك · ٣٠ث متوقف · ٦٠ث خامل · **صفر وهو غير متاح**)، الرفع المجمَّع كل ١٥ث مع احتفاظ بالنقاط عند الفشل، سلّم الصلاحيات نافذة نافذة، ودليل البطارية لكل مُصنِّع.
- **`:feature:availability`**: مفتاح واحد لكل المطاعم. **لا تفاؤل إطلاقاً** — الحالة لا تصير إلا ما أكّده الخادم، والنقرة الثانية أثناء الطيران محجوبة فيزيائياً. و`409 has_active_trip` رسالته أنه يُنهى رحلته لا أنه أخطأ.
- **`:app`**: شريط سفلى بوجهتين («متاح» / «مطاعمى») — صار مبرَّراً بوجود وجهتين حقيقيتين، وما زال `when` لا `NavHost` (لا شىء deep-linked بعد).

**🔴 عطلان لم يكشفهما إلا الجهاز — سجّلهما كسابقة:**
1. **النقر على «متاح» كان يُسقط التطبيق.** طلب الصلاحية **غير متزامن**، وبدء خدمة أمامية من نوع `location` قبل وصول المنح يرمى `SecurityException` تقتل العملية على أندرويد ١٤+. الحل: تُحفظ النيّة (`wantsTracking`) و`LaunchedEffect` يبدأ الخدمة حين تصل الصلاحية فعلاً. والخدمة نفسها تلتقط الاستثناء و`stopSelf()` بدل أن تقتل التطبيق لو سُحبت الصلاحية من شريط الإشعارات وهى تعمل.
2. **لافتة «مسموح أثناء فتح التطبيق» كانت خضراء بمثلث تحذير.** الأخضر يقول «تمام» والمثلث يقول «خطأ»، ومعاً لا يقولان شيئاً. الحالة ليست أياً منهما: الموقع يعمل وهناك تحفّظ. صارت سطحاً محايداً بعلامة معلومة، والأحمر محجوز للرفض الكامل وحده.

**🔴 وإصلاح من مراجعة شغل الوكلاء:** الكنس كان يترك عضو GEO بائتاً لسائق تعذّر إخراجه (رحلة جارية). عضو GEO **لا ينتهى من نفسه**، فأول ما تُغلق تلك الرحلة كان الموزّع يعرض الطلب التالى على جوال ميت عند ناصية غادرها بساعات. مُغطّى باختبار **مُتحقَّق أنه يفشل بدون الإصلاح**.

~~**التالى مباشرةً:** الأسبوع ٣…~~ — **أُنجز فى جلسة 2026-09-21 الثالثة** (انظر «المُنجَز» أعلى الملف). **وما زال معلّقاً منها: الاختبار الميدانى** (الجوال فى الجيب و«متاح» ٦٠ دقيقة ثم فحص `last_seen_at`) — لا بديل عنه ولم يُنفَّذ بعد.

**🔴 انحرافان عن العقد وقعا فعلاً وأُصلحا فى نفس الجلسة** (مراجعة قائد الجلسة لشغل الوكلاء — لا تسلّم بتقرير وكيل):
- `login` كان يُصدر توكناً لسائق **موقوف** أو شركته منتهية، والعقد يوثّق خمسة أكواد 403 على **هذا** المسار بالذات. شاشة الدخول هى المكان الوحيد الذى يتصرّف فيه السائق إزاء «حسابك موقوف»؛ إعطاؤه توكناً وإفشال النداء التالى يعرض خطأ عاماً على شاشة غادرها بالفعل.
- رابط شهادات جوجل كان يحمل `metadata/x509` **مكرراً** ⇒ كل تحقّق من توكن Firebase كان سيفشل، ولا يظهر العطل إلا يوم يُضبط المشروع.
- وثالثة أصغر: مقارنة رقم الإصدار كانت نصّاً مقابل عدد صحيح ⇒ «تغيّر البناء» صحيحة فى **كل** طلب فتتحوّل كبحة «مرّة كل دقيقة» إلى كتابة قاعدة بيانات لكل نداء.

**التالى مباشرةً:** ركّب `google-services.json` لمنيورا فى **`app/src/meniura/`** (لا `app/` — وإلا أخذته نكهة تاج أيضاً)، أضف `firebase-auth`+`firebase-messaging`، ثم شغّل على S25 مقابل الباك اند المحلى (`adb reverse` + `driver.debugBaseUrl`) وأثبت الدورة كاملة: `branding` ⇒ رمز حقيقى ⇒ `verify-otp` ⇒ `set-password` ⇒ `login` ⇒ `/driver/me`.

- **شاشات المصادقة الثلاث مبنية** (`:feature:auth`، غير مُلتزَمة) — وقرار صاحب المشروع غيّر شكلها جذرياً (2026-09-21):
  - **«رمز سرّى ٤ أرقام» صار «كلمة مرور» (٦ أحرف فأكثر)** — قرار ١٧ مُعدَّل. **العقد اتغيّر معاه**: `pin` → `password`، و`set-pin` → `set-password`، و`needs_pin` → `needs_password`. وهذا بالضبط سبب كتابة العقد قبل الباك اند: تصحيحه اليوم بلا كلفة.
  - **🔴 التسمية هى التى سبّبت اللبس كله:** كنت أسمّى حقل الدخول «الرمز السرّى»، فقرأه صاحب المشروع كأنه رمز الرسالة وطلب خانات. **القاعدة الآن:** ما يأتى بالرسالة اسمه **«رمز»** ويُكتب فى **خانات مكشوفة بلا عين**؛ وما يدخل به السائق بعدها اسمه **«كلمة المرور»** وهو **حقل واحد مخفى بعين**. لا تخلط الاسمين مرّة أخرى — الشكل يتبع الاسم.
  - **`DigitCellsField` فى `:core:ui`:** خانة لكل رقم، الطول معلوم فيقوله الحقل قبل أن يكتب السائق حرفاً. **لا تقنيع ولا عين للرمز** — الرسالة واصلة فى شريط الإشعارات فوق الحقل بسنتيمترين، فإخفاؤها لا يحمى شيئاً ويمنعه من مراجعة ما كتبه.
  - **🔴 فايربيز لا يرسل من نفسه** — التطبيق هو الذى يطلب. فكان السائق المدعوّ حديثاً **مقفولاً بلا مخرج**: رقم وكلمة مرور لا يملكها ولا طريق لصنعها. أُضيف **«أول مرة؟ اطلب رمز التحقق»** تحت الزر، و**فتح شاشة الرمز هو الطلب نفسه** (`OtpViewModel.start`، محروس بعلم كى لا تُصرف رسالة ثانية عند إعادة التركيب).
  - **إثبات الجوال هو الاستعادة** — لا رابط استعادة ولا endpoint منفصل: `set-password` يُستدعى على الجلسة التى فتحها `verify-otp`. ولذلك `verify-otp` صار **يُرجِع توكناً** (تعديل عقد): بدونه كان السائق يُطالَب بإثبات نفس الجوال مرّتين فى جلسة واحدة.
  - **الحقول الحمراء تعنى خطأ السائق فقط:** خانات الرمز تحمرّ عند `otp_invalid`/`otp_expired` **لا عند انقطاع الشبكة** — تلوينها لانقطاع يطلب منه مراجعة أرقام كانت صحيحة.
  - **عدّاد إعادة الإرسال يبدأ عند النجاح فقط** — طلب لم يغادر الجهاز لم يُرسل رسالة، وإجباره على انتظار ٤٥ث لرسالة غير موجودة عكس الغرض.
  - **«أدخل رقمك» و«الرقم غير صحيح» رسالتان مختلفتان** — الأولى خطوة ناقصة والثانية رقم خاطئ، وإخبار السائق بإدخال رقم يراه أمامه يُفقده الثقة بالتطبيق.
  - **`GET /driver/branding` مُتحقَّق بصرياً بشعار منيورا الحقيقى** (خادم وهمى محلى بنفس حمولة الإنتاج؛ الشعار نفسه حُمِّل من `app.meniura.com`). الشعار **44dp** بطلب صاحب المشروع.

**⏳ ينتظر مراجعة ناطق أصلى:** كل نصوص ur/bn/hi (الأخطاء الـ٢٩ + شاشتا اللغة والدخول) — كتبها الوكيل.

**معلّق على صاحب المشروع:** مشروع Firebase **أُنشئ** (`meniura-driver`، Blaze، الحزم الثلاث مسجَّلة) لكن **بلا أى بصمة SHA-1 ⇒ Phone Auth لا يعمل بعد** (بصمة الـdebug: `31:CA:84:12:A6:EF:4C:3F:24:5A:D5:78:38:09:CF:93:5E:66:A3:74`)، والملف المُنزَّل يحمل **حزمة الإصدار وحدها** فيلزم إعادة تنزيله بالثلاث · ولا مشروع لتاج بعد · (الباقى كما هو: مشروع Firebase لتاج) · keystore · مفتاح خرائط · Sentry DSN · الأيقونة الحقيقية (الحالية **مؤقتة منسوخة من POS**) · أول مطعم مستخدم.

### 🟢 بروتوكول **بداية** الجلسة (إلزامى — قبل أى عمل)
1. اقرأ هذا القسم كاملاً، ثم `CLAUDE.md` كله.
2. نفّذ وأعلن النتيجة للمستخدم بالعربية:
   ```bash
   git log --oneline -5 && git status --short && git log origin/main..HEAD --oneline
   ```
3. **قارن الواقع بالمكتوب أعلاه.** اختلفا ⇒ **الكود والـgit هما الحَكَم**، وصحّح القسم فوراً.
4. اذكر للمستخدم فى سطرين: **أين وقفنا** و**ما سأفعله الآن**، ثم ابدأ.
5. إن وجدت عملاً غير مُلتزَم من جلسة سابقة: **لا تبنِ فوقه قبل أن تفهمه وتُعلم المستخدم**.

### 🔴 بروتوكول **نهاية** الجلسة (إلزامى — لا تُنهِ جلسة بدونه)
**قبل آخر رسالة للمستخدم، حدّث جدول «أين وصلنا» أعلاه بـ:**
- التاريخ · المرحلة والخطوة · آخر commit (hash + العنوان) · هل دُفع · حالة الشجرة · **أرقام الاختبارات الخضراء**.
- **المُنجَز** فى هذه الجلسة (بنود قصيرة).
- **التالى مباشرةً** — جملة واحدة قابلة للتنفيذ بلا تفكير («أضف شاشة X» لا «أكمل المرحلة ١»).
- **أى عمل غير مُلتزَم** ولماذا.
- **ما ينتظر صاحب المشروع**.
- أى قرار جديد ⇒ «سجلّ القرارات» · أى مزلق كلّفك >30 دقيقة ⇒ «المزالق».

**ثم التزم التحديث مع الكود فى نفس الـcommit.** جلسة تنتهى بلا هذا = الجلسة التالية تبدأ بالتخمين، وهذا بالضبط ما يُنتج إعادة البناء.

---

## ⛔ القواعد الحاكمة (لا استثناء)

### 🌐 لغة التواصل
- **🔴 كل كلمة موجّهة للمستخدم بالعربية — بلا أى استثناء.** الرد النهائى، **ورسائل المتابعة القصيرة بين الأدوات** («أبنى الآن…»)، والأسئلة، والجداول، والشرح، وأسباب القرارات.
- الإنجليزية مسموحة **فقط** فى: أسماء الملفات والدوال والأوامر والمسارات داخل `code`، والكود وتعليقاته، ورسائل الـcommits.

### 🤖 الوكلاء الفرعيون
- **Sonnet 5 حصراً** (`model: "sonnet"`) **وبحد أقصى 3 متوازين**. لا تفترض نطاقاً أكبر حتى لو كان `ultracode` مفعّلاً.
- **كل وكيل فى وحدة Gradle مختلفة** — وكيلان فى نفس الوحدة يُفشلان اختبارات بعضهما.
- **ممنوع على الوكلاء:** `commit`، فرع، `git stash/checkout/reset`، تعديل `CLAUDE.md`، تعديل الباك اند بلا تكليف صريح.
- راجع كل حكم «ليس خللاً» يصدره وكيل بنفسك.
- استخدم أداة `Agent` مباشرة لا `Workflow`، إلا بطلب صريح.

### 🌿 Git والتحقق
- **🔴 لا فروع — العمل على `main` مباشرةً.** حتى لو طُلبت مراجعة: اترك التغيير غير مُلتزَم حتى يوافق صاحب المشروع.
- **🔴 لا CI ولا فحوصات على GitHub.** لا تُنشئ `.github/workflows`. كل التحقق **محلياً وعلى جهاز حقيقى** قبل أى commit.
- **لا `push` قبل إخبار صاحب المشروع بنتيجة التحقق وموافقته.**
- `CLAUDE.md` و`.claude/` **متتبَّعان هنا** (لا خادم إنتاج يسحب هذا الريبو). ⚠️ عكس ريبو الباك اند حيث `CLAUDE.md` متجاهل عمداً.
- الريبو: `https://github.com/mazenelmasry/driver-android-saas-whatsapp-qrmenu.git` · الفرع `main`.
- رسائل الـcommit إنجليزية `type(scope): what`.

### 🚫 ممنوعات مطلقة
- لا دومين ولا مفتاح API ولا معرّف حساب مكتوب فى الكود. الدومين من الـflavor، والأسرار من `local.properties`.
- لا تطبيق باسم كل مطعم. **تطبيق واحد لكل منصة.**
- لا كتابة موقع السائق فى MySQL لكل نبضة — **Redis GEO**.
- لا `CircularProgressIndicator` وسط الشاشة · لا `tween()` لحركة يقودها المستخدم · لا شاشة بلا حالاتها الأربع.
- لا نصّ حرفى داخل Composable · **لا رقم dp/sp حرفى** خارج `Tokens.kt`.
- لا تعديل عقد API فى طرف واحد — الريبوان معاً + `openapi/driver.v1.yaml` + إعادة توليد المرآة.
- لا `migrate:fresh`/`refresh`/`reset`/`db:wipe` فى الباك اند أبداً.

---

## 🧭 النموذج — اقرأه قبل أى شىء

### 🔴 السائق **مستقل** لا موظف
فى السعودية المطاعم **لا توظّف سائقين**؛ السائق يعمل أصلاً على تطبيقات التوصيل، ويُحمِّل تطبيق المطعم ليأخذ منه توصيلات **بالقطعة**. وهذا يحكم كل قرار:
- يرى **ما كسبه** — من يعمل بالقطعة لا يستخدم تطبيقاً يخفى عنه أجره.
- قد يعمل **مع أكثر من مطعم** على منيورا فى وقت واحد.
- يدخل **بجواله** لا ببريده.
- **لا راتب ولا ورديات** — «متاح / غير متاح» فقط.

### المراحل
```
المرحلة ١  ← سائقو المطعم (الأولوية القصوى)            ← نبنيها الآن
المرحلة ٢  ← توزيع متقدّم (ETA حقيقى، موجات، تجميع)
المرحلة ٣  ← جودة ومال (كشف أجور للمطعم، OTP تسليم، تقييم)
المرحلة ٤  ← أسطول المنصة (سائقون تابعون لمنيورا)      ← بعتبة رقمية + رأى قانونى
```
- **🔑 المرحلة ١ تقيس سوق المرحلة ٤ مجاناً:** كل مرة يحين فيها وقت إرسال طلب ولا سائق متاح ⇒ حدث `no_driver_available`. **عتبة الإطلاق: ≥15٪ من طلبات التوصيل فى حىّ واحد + طلب صريح من 5 مطاعم + رأى قانونى مكتوب.**
- **مفهوم «الأسطول» يُبنى من اليوم الأول** (`fleets` بنوع `restaurant` الآن و`platform` لاحقاً).

### الأولوية الآن
> **«أى مطعم يريد تشغيل سائقيه — أشغّله بسرعة.»** كل قرار يُقاس بهذا.

---

## 📂 خريطة المشاريع (أربعة ريبوهات)

| المشروع | المسار | الدور |
|---|---|---|
| **الباك اند** (Laravel 12 + Filament v5) | `C:\laragon\www\filamentv4-saas-whatsapp-qrmenu` | **مصدر الحقيقة لكل عقد `/api/v1/driver/*`** |
| الفرونت اند (Next.js 16) | `C:\laragon\www\qrmenu-nextjs-saas-whatsapp` | المنيو + صفحة تتبّع الطلب للعميل |
| نقطة البيع (Android) | `C:\laragon\www\pos-android-native-saas-whatsapp-qrmenu` | **المرجع الذى تُنسخ منه الأنماط** |
| **تطبيق السائقين (هنا)** | `C:\laragon\www\driver-android-saas-whatsapp-qrmenu` | |

- **منصتان:** `app.taaj.me` و`app.meniura.com` — الدومين من الـflavor فقط.
- أى ميزة تمتد عبر الريبوين تُعدَّل فيهما معاً، وcommit/push **منفصل لكل ريبو**.
- **اقرأ `CLAUDE.md` فى ريبو الباك اند** قبل أى تعديل هناك.
- **لا تعتمد على ريبو POS كوحدة مشتركة** — انسخ الأنماط ملفاً ملفاً. وحدة مشتركة يعدّلها وكيل تكسر كاشير عملاء يدفعون. **نطاق الضرر هو المعيار حين لا يوجد مراجع بشرى.**
- ⚠️ **انسخ من `main` فى ريبو POS — لا من أى فرع.** إعادة تصميم المرحلة ٥ (**Material 3**) هى التصميم المعتمد و**مدموجة بالكامل فى `main`** (`2bcf1be8`)، و`main` متقدّم عليها بـ9 commits. الفرع `redesign/pos-expressive` **بقايا مكرّرة** لا يحمل شيئاً ليس فى `main` (مُتحقَّق: `main...redesign = 9/0`). و`CLAUDE.md` هناك ما زال يصفه كعمل جارٍ غير مدفوع — **الكود هو الحَكَم لا الوثيقة.**

---

## ✅ الموجود فعلاً فى الباك اند (لا تُعِد بناءه)

مُتحقَّق بالفحص 2026-09-21:

| الموجود | أين |
|---|---|
| دور `delivery_driver` | `database/seeders/PermissionSeeder.php` |
| شاشة «سائقو التوصيل» (اسم/جوال/بريد/كلمة مرور/فرع/نشط) | `app/Filament/Company/Resources/DeliveryDrivers/*` |
| إجراء «تعيين/إعادة تعيين سائق» | `ViewOrder::assign_driver` + `CompanyOrderActionController::assignDriver` |
| إشعار السائق عند التعيين | `app/Jobs/SendDriverAssignedNotification.php` |
| `OutForDelivery`/`Delivered` + طوابعهما الزمنية | `OrderStatus`, `Order` |
| عرض السائق للعميل (مشروط بإعداد الشركة) | `Api\OrderResource::formatDeliveryDriver` |
| **نمط التوكن المخصّص (لا Sanctum)** | `PosStaffToken` + `PosStaffAuth` + `PosBroadcastingController` |
| هوية المنصة | `GET /api/v1/pos/branding` (عام) |
| Redis (predis) + Reverb + OneSignal | `.env`, `OneSignalService` |
| **مناطق التوصيل برسومها** | `DeliveryZone` · `orders.delivery_fee` |
| زمن تحضير الصنف | `products.preparation_time` |
| نمط «قائمة الفروع قبل الدخول» | `POST pos/branches-for-user/{slug}` |

---

## 🧊 القرارات المُجمَّدة

| # | القرار | القيمة |
|---|---|---|
| 1 | اللغة/الإطار | **Kotlin + Compose أصلى** · ريبو منفصل · أنماط منسوخة من POS |
| 2 | الحزمة | `app.qrmenu.driver` + لاحقة لكل نكهة `.meniura` / `.taaj` |
| 3 | النكهات | `meniura` → `app.meniura.com` · `taaj` → `app.taaj.me` |
| 4 | minSdk / target / compile | **26 / 35 / 35** |
| 5 | اسم التطبيق | **«منيورا للسائقين» / «Taaj Drivers»** |
| 6 | الأيقونة | **شعار المنصة + شارة دراجة/شاحنة** |
| 7 | الهوية | **المنصة** — والمطعم محتوى داخل بطاقة الطلب |
| 8 | اللغات | **ar · en · ur (RTL) · bn · hi** |
| 9 | اللغة الافتراضية | **شاشة اختيار أولاً** ثم العربية |
| 10 | الخط | **IBM Plex Sans Arabic** (نفس POS) للعربى/الإنجليزى/الأردو + خط النظام للبنغالى/الهندى |
| 11 | التوزيع | **مُحدِّث ذاتى** — خادمنا يستضيف APK وبانر يُجبر التحميل والتثبيت |
| 12 | مستوى الإجبار | **مستويان:** «أدنى إصدار» يمنع بشاشة كاملة · «أحدث إصدار» بانر يُذكّر |
| 13 | الإشعارات | **FCM مباشر** (لا OneSignal لهذا التطبيق) |
| 14 | ضمان الوصول | **ACK:** لا إقرار ١٥ث ⇒ إعادة إرسال · ٤٥ث ⇒ إعادة توزيع |
| 15 | إنشاء الحساب | **دعوة من المطعم** (لا تسجيل ذاتى) |
| 16 | قناة الدعوة | **واتساب من اللوحة + كود يُملى شفهياً** |
| 17 | الدخول | **الجوال + كلمة مرور** (٦ أحرف فأكثر، يختارها السائق) · OTP بـFirebase أول مرة ⚠️ **غُيِّر 2026-09-21** — كان «رمز سرّى ٤ أرقام» |
| 18 | استعادة الدخول | **OTP الجوال ثم اختيار كلمة مرور جديدة** + زر إعادة تعيين للمطعم. لا رابط استعادة — إثبات الجوال هو الاستعادة |
| 19 | تعدد المطاعم | **مدعوم من المرحلة ١** — دخول بالجوال يعرض كل مطاعمه |
| 20 | التوفّر | **متاح لكل مطاعمه معاً** |
| 21 | سقف الطلبات المتزامنة | **طلب واحد** |
| 22 | إطفاء «متاح» تلقائياً | **عند إغلاق الفرع فقط** |
| 23 | قبل قبول الطلب يرى | **الحىّ + المسافة + المبلغ** (العنوان والجوال بعد القبول) |
| 24 | طريقة الربط الافتراضية | **يدوى** — والأربعة كلها فى إعدادات الفرع |
| 25 | توقيت الإرسال الافتراضى | **عند الجاهزية** — والأربعة كلها فى إعدادات الفرع |
| 26 | نصف قطر البحث | **٥ كم** يضبطه المطعم |
| 27 | ترتيب المرشّحين | **المسافة + نسبة القبول** |
| 28 | لا سائق قَبِل | **يبقى معروضاً + إشعار للمطعم** + `no_driver_available` |
| 29 | مهلة قبول العرض | **٤٥ ثانية** |
| 30 | أجر التوصيلة | **= رسوم منطقة التوصيل**، والمطعم يعدّلها لكل منطقة |
| 31 | استحقاق الأجر | **عند التسليم** + زر «أجر رحلة فاشلة» للمطعم بسبب |
| 32 | الدفتر المالى | **المرحلة ١** — مقاصّة (أجور − نقد محصَّل) |
| 33 | السائق يرى | **دفتره كاملاً** (أجوره + النقد بحوزته + الصافى) |
| 34 | سقف نقدى | **نعم يضبطه المطعم** (صفر = بلا سقف) |
| 35 | كشف أجور للمطعم | **المرحلة ٣** (للمطعم وحده لا للسائق) |
| 36 | مسار الرحلة | **يُسجَّل مختصراً** ويُحذف بعد 30 يوماً |
| 37 | إشعار المطعم | **التسليم والمشاكل و«لا سائق» فقط** |
| 38 | سجلّ السائق | **٧ أيام** + عدّاد اليوم |
| 39 | شاشة تدريب | **نعم** 4–5 شرائح + زر «مساعدة» دائم |
| 40 | رفض صلاحية الموقع الخلفى | **يعمل مع لافتة حمراء دائمة** |
| 41 | بوّابة الخطة | **ميزة جديدة `DeliveryManagement` تُباع منفصلة** |
| 42 | الخرائط | **صورة ثابتة + ملاحة خارجية** فى المرحلة ١ |
| 43 | أجهزة بلا خدمات جوجل | **غير مدعومة** + رسالة واضحة |
| 44 | أول نسخة | **~٣ أسابيع:** دخول + متاح + طلبات حقيقية + دورة رحلة كاملة |
| 45 | دولة السائق | **لا يختارها — مستنتَجة من الدعوة** (`companies.country_code` للمطعم الداعى). لا حقل دولة على `drivers` ولا شاشة اختيار دولة. |
| 46 | نطاق السائق الجغرافى | **مستنتَج أيضاً:** `driver_restaurant_links.branch_ids` (المطعم يحدّده) + نصف قطر البحث (#26) + الترتيب بالمسافة (#27). **لا اختيار منطقة من السائق.** |
| 47 | شاشة «لا طلبات» | **يجب أن تقول السبب بالاسم** (فروعى المرتبطة / خارج النطاق / الفرع مغلق) — لا شاشة فراغ صامتة. |

### 🔴 قرار ٤٥–٤٧ — لماذا لا يختار السائق دولته ولا منطقته (2026-09-21)

> السؤال المطروح: «المنصة تخدم عدة دول، فيجب أن يختار السائق الدولة أول مرة ويرتبط بها، ومن يسجّل فى السعودية يختار المنطقة ليرى طلباتها.»

- **المقدّمة التى يستند إليها السؤال غير قائمة فى هذا النموذج:** لا تسجيل ذاتى (قرار ١٥) — **المطعم هو من يدعو**. فالسائق لا يوجد فى النظام إلا ومعه شركة داعية، وللشركة `country_code`، وللرابط `branch_ids`. **الدولة والمنطقة معلومتان قبل أن يفتح التطبيق.**
- **الاختيار اليدوى يخلق مصدر حقيقة ثانياً يمكن أن يناقض الأول**، وأسوأ أعراضه صامت: سائق دعاه مطعم سعودى واختار «مصر» ⇒ **صفر طلبات إلى الأبد، وشاشة لا تشرح شيئاً**. وهذا بالضبط العطل الذى أراد السؤال منعه.
- **الجغرافيا مُصفّاة أصلاً:** المرشّح يُختار داخل نصف قطر الفرع (#26، افتراضه ٥ كم) ويُرتَّب بالمسافة (#27) ⇒ سائق الرياض **لا يصير مرشّحاً** لفرع جدّة ولو كان مرتبطاً بالشركة كلها. واختيار منطقة من السائق كان **سيناقض قرار ٢٠** («متاح لكل مطاعمه معاً») ويضيف مفتاحاً يُطفئ به رزقه بالخطأ.
- **العابر للدول آمن بلا حقل جديد:** الدفتر لكل (سائق × مطعم) منفصل (قاعدة ٢) ⇒ العملة تتبع الشركة، ولا مقاصّة عبر الدول بالبناء.
- **✅ ما يجب أن يُبنى فعلاً بدلاً منه (قرار ٤٧):** شاشة «لا طلبات الآن» **تسمّى السبب**: أسماء فروعى المرتبطة، وأننى خارج نطاق أقرب فرع (بالمسافة)، أو أن الفرع مغلق (قرار ٢٢)، أو أن «متاح» مطفأ. الفراغ الصامت هو ما يجعل السائق يظن أن التطبيق معطَّل فيطلب اختيار دولة.
### ⏭️ أسطول المنصة (المرحلة ٤) — هناك **يختار السائق الدولة والمنطقة** (تأكيد صاحب المشروع، 2026-09-21)

القرار ٤٥ يخصّ **مسار الدعوة وحده**. فى أسطول المنصة (`fleets.type = platform`) يسجّل السائق نفسه بلا مطعم داعٍ، **فلا يوجد ما يُستنتَج منه** — وعندها **يختار دولته ومنطقته عند التسجيل ويرتبط بهما**، وتُعرض عليه طلبات مطاعم تلك المنطقة. هذا ليس تناقضاً مع ٤٥ بل نتيجته: **الاختيار اليدوى يوجد حيث لا توجد دعوة تُستنتَج منها، ولا يوجد حيث توجد.**

ما يلزم تجهيزه حين تُبنى المرحلة ٤ (لا تبنِ منه شيئاً الآن، لكن **لا تبنِ اليوم ما يستحيل تمديده غداً**):
- `drivers` يحتاج حينها `country_code` + مرجع منطقة (`region`/`city_id`) **nullable بلا backfill**: `null` = «سائق دعوة، دولته من رابطه» ⇒ لا ينكسر أى سائق قائم. **لا تضع `country_code` على `drivers` قبل ذلك** — عمود يبقى فارغاً سنة يُقرأ خطأً.
- `fleets` موجود فى النموذج أصلاً (`type[restaurant|platform]`)، فسكّة الأسطولين مفتوحة بلا هجرة كاسرة.
- **الدولة تُقفَل بعد التسجيل** كما تُقفَل عملة الشركة فى الباك اند (سابقة موثَّقة): الدولة تحدّد العملة والدفتر وسياسة الدفع، وتغييرها لاحقاً يعنى دفتراً بعملتين.
- المنطقة تُشتق من **نفس مرجع المناطق المستخدَم فى الباك اند** (`service_areas`/`delivery_zones`/`cities`) لا من قائمة نصوص جديدة — وإلا صار للمنصة تعريفان للمنطقة لا يتطابقان.
- يحتاج أيضاً: تحقّق هوية، سياسة قبول، وعقداً مالياً **مع المنصة لا مع المطعم** (الدفتر الحالى لكل «سائق × مطعم»).

---

## 🎨 نظام التصميم — منسوخ من POS مع أربعة انحرافات مقصودة

**المصدر:** `pos-android-native-saas-whatsapp-qrmenu/core/designsystem/src/main/.../theme/` **على `main`** — نظام **Material 3** ناضج (إعادة تصميم 2026-09-19، مدموجة ومستقرّة وهى التصميم المعتمد). انسخ بنيته كما هى.
- **اللغة التصميمية: Material 3** — `lightColorScheme`/`darkColorScheme` + `MaterialTheme` + `Shapes` + `Typography`. لا تخترع نظاماً موازياً ولا تستخدم Material 2.

### ما يُنسَخ كما هو
| الملف | المحتوى |
|---|---|
| `Tokens.kt` | `Spacing` (شبكة 8dp: 4/8/12/16/20/24/32/40/48/64) · `Radius` (chip 8 · card 12 · sheet 16 · fab 28 · pill 999) · `Elevation` (0/1/3/8/12) · `TouchTarget` · `Motion` |
| `Theme.kt` | بناء `ColorScheme` كامل من **تسعة ألوان لكل نكهة** فى `app/src/<flavor>/res/values/driver_brand_colors.xml`؛ الأسطح **تُشتقّ بالتدرّج (`lerp`) بين الورق والحبر** فتنتمى للعلامة بدل بنفسجى Material · `Shapes` = 8/12/16/24/28 |
| `AccentPalette.kt` | يشتقّ عائلة اللون من بذرة واحدة **مع حارس تباين 4.5:1** (يُغمِّق اللون حتى يصير النص الأبيض مقروءاً) |
| `Type.kt` | بنية الخطوط والأوزان الأربعة |
| `core/ui/components/` | `ModalScaffold` · `ModalChrome` · `Shimmer` · `ListLoadingSkeleton` · `InlineErrorBanner` · `NumericKeypad` · `PosSnackbarHost` → `DriverSnackbarHost` |

### 🔴 الانحرافات الأربعة — لا تنسخها من POS
| POS | صحيح هناك لأن… | وخطأ هنا لأن… | القرار |
|---|---|---|---|
| `fontScale = 1f` مثبّت فى `PosTheme` | تخطيط تابلت ثابت والفواتير تنكسر مع التكبير | **يمنع السائق كبير السنّ من تكبير الخط** | **نحترم خط النظام حتى 200٪** ونختبر عليه |
| فاتح دائماً، بلا مخطط داكن | الكاشير تحت إضاءة المحل | **السائق يعمل ليلاً** | **وضع ليلى كامل مصمَّم** + تبديل تلقائى حسب النظام |
| `PosUiScale` (حجم لكل جهاز) | تفضيل مقاس التابلت | لا معنى له على جوال | **لا يُنسخ** — خط النظام يغنى عنه |
| منتقى لون لكل جهاز | الكاشير يخصّص طرفيته | هوية السائق = هوية المنصة | **لا منتقى**؛ اللون من النكهة + `accent_color` من لوحة الأدمن |

### الخط — `IBM Plex Sans Arabic` (مقيس لا مفترَض)
تُنسخ ملفات `res/font/ibm_plex_sans_arabic_{regular,medium,semibold,bold}.ttf` من POS كما هى (OFL).

**قياس فعلى على الملف بـ`fontTools` (2026-09-21):**

| | IBM Plex Sans Arabic | Cairo (بديل مرفوض) |
|---|---|---|
| عربى + لاتينى + أرقام | ✅ | ✅ |
| **الأردو — الـ12 حرفاً المختبَرة كلها** (ٹ ڈ ڑ ں ھ ہ ی ے ک گ چ ژ) | ✅ | ✅ |
| بنغالى · هندى | ❌ | ❌ |
| **عدد المحارف** | **902** | 701 |

- 🔴 **لا يوجد خط عربى واحد يغطّى الخمس** — ولا يمكن أن يوجد.
- ✅ **ولا شىء ينكسر:** أندرويد يستبدل **لكل محرف ناقص** تلقائياً بخط النظام (Noto) ⇒ البنغالى والهندى يظهران سليمين بلا تضمين. **لا تضمّن خطوطاً إضافية** (حجم بلا عائد).
- **لماذا IBM Plex لا Cairo:** 902 محرفاً مقابل 701 (مربعات ناقصة أقل فى الأسماء والعناوين) · **نفس خط POS** فيبدو تطبيقاك عائلة واحدة على جهاز صاحب المطعم · واختير هناك لأرقامه اللاتينية الحادة — وهذا التطبيق كله أرقام (مبالغ، مسافات، عدّاد، جوالات).
- **الأرقام لاتينية دائماً** عبر `LocaleManager` (`-u-nu-latn`) + مُنسِّق مال مركزى.
- ⚠️ **تحقّق بصرياً من الأردو والبنغالى على الجهاز فى الأسبوع الأول** — التغطية بالمحارف ليست ضماناً للتشكيل (GSUB) ولا لارتفاع السطر.

### قواعد الواجهة (جوال السائق ≠ تابلت الكاشير)
- **يد واحدة، بقفّاز أحياناً، وتحت الشمس:** أزرار الرحلة **≥ 64dp** بعرض الشاشة · تباين عالٍ · خط كبير للعنوان والمبلغ.
- **الحالات الأربع إلزامية** لكل شاشة: هيكل تحميل (لا دوّار) · فارغ مصمَّم · خطأ **داخلى** بإعادة محاولة · **بلا اتصال**.
- **أهم ٣ معلومات بلا تمرير:** الفرع · عنوان العميل · المبلغ المطلوب تحصيله.
- **الحركة `spring()` فقط**؛ `tween` للحلقات المحيطة (shimmer) لا غير.
- **RTL** لـar/ur · **LTR** لـen/bn/hi. الأرقام والهواتف والمبالغ **معزولة الاتجاه** (`BidiText.ltr`).
- `@Preview(locale=…)` لكل لغة على كل شاشة، **وفى الوضعين الفاتح والداكن**.
- ⚠️ **أسماء المطاعم والعناوين تأتى بالعربى فقط من القاعدة** — فزرّا الخريطة والاتصال أبرز من نصّ العنوان.

---

## 🗄️ نموذج البيانات — الهوية متعددة المطاعم

🔴 **المشكلة:** `users` مربوط بشركة واحدة، والسائق المستقل يعمل لعدة مطاعم.
🔴 **الحل:** طبقة هوية فوق `users` — و**`orders.delivery_driver_id` يبقى يشير إلى `users`** (صفر تغيير على كود الطلبات وشاشة Filament القائمة).

```
drivers                   (phone_canonical UNIQUE, name, is_active, locale, fcm_token,
                           app_version, last_seen_at, last_lat, last_lng,
                           is_online, online_since, acceptance_rate)
driver_restaurant_links   [unique(driver_id, company_id)]
                          (driver_id, company_id, user_id → users.id, branch_ids json,
                           status[invited|active|suspended], invite_code, invited_at, joined_at)
fleets                    (company_id nullable, type[restaurant|platform], name, is_active)
driver_tokens             (driver_id, token(sha256), device_name, app_version,
                           app_platform, last_used_at, expires_at)
driver_offers             (order_id, driver_id, wave, sent_at, acked_at, expires_at,
                           outcome[accepted|declined|expired|superseded], decline_reason)
driver_ledger_entries     (driver_id, company_id, order_id?, type, amount, balance_after,
                           created_by_user_id?, note, created_at)
   types: delivery_fee_earned · cash_collected · failed_trip_fee · settlement · manual_adjustment
driver_settlements        (driver_id, company_id, amount, direction[to_driver|from_driver],
                           settled_by_user_id, period_from, period_to, note)
trip_breadcrumbs          (order_id, points json, created_at)   ← تُحذف بعد 30 يوماً
order_delivery_events     (order_id, driver_id?, branch_id, type, meta json)
   types: dispatch_scheduled · dispatch_fired · offered · offer_acked · offer_declined ·
          offer_expired · assigned · claimed · released · picked_up · delivered ·
          issue · no_driver_available
orders                    + driver_claimed_at, driver_offer_expires_at, dispatch_due_at,
                            delivery_cash_collected, driver_fee (لقطة)
```

### 🔴 قواعد ملزمة
1. **`orders.delivery_driver_id` يبقى FK إلى `users`** — عند القبول نكتب `user_id` الخاص بالرابط مع **تلك الشركة**.
2. **الدفتر لكل (سائق × مطعم) منفصل تماماً** — لا مقاصّة عبر المطاعم.
3. **`orders.driver_fee` لقطة تُختم وقت الإنشاء:** `المطعم ضبط أجراً للمنطقة ? ذلك : رسوم التوصيل المحسوبة`. ⚠️ **لا تقرأ الرسوم الحيّة وقت التسليم** — عرض «توصيل مجانى» يصفّرها فيضيع أجر السائق.
4. **`phone_canonical`** بنفس تطبيع الباك اند (libphonenumber، E.164).
5. `drivers.is_active` يوقفه عند كل المطاعم؛ `driver_restaurant_links.status` عند مطعم واحد.

---

## 💰 الدفتر المالى — المقاصّة (قلب المرحلة ١)

| | من يدين لمن |
|---|---|
| طلب **بالبطاقة** (23 طعام + 10 توصيل = 33 دخلت حساب المطعم) | **المطعم يدين للسائق** بأجر التوصيلة |
| طلب **نقدى** (السائق قبض 33) | **السائق يدين للمطعم** بقيمة الطعام |

```
رصيد السائق عند مطعم =  Σ(أجور توصيلاته)  −  Σ(النقد الذى حصّله)
موجب ⇒ المطعم يدفع له   ·   سالب ⇒ السائق يورّد
```
**مثال يوم:** 5 فيزا × 10 = +50 · 3 نقدية (حصّل 99) × 10 = +30 −99 ⇒ **الصافى −19 ⇒ يورّد 19**.

- **✅ المال لا يمرّ بمنيورا إطلاقاً** — نحسب الرقم فقط. لا ترخيص ولا محفظة.
- **الأجر يُقيَّد عند `delivered` فقط.** رحلة فشلت ⇒ زر «اصرف أجر رحلة فاشلة» يقيّد `failed_trip_fee` **بسبب إلزامى**.
- **زر «تسوية»:** المدير يُدخل المبلغ والاتجاه ⇒ يُصفَّر الرصيد ويُسجَّل الفرق (عجز/زيادة).
- **السقف النقدى** (`branches.driver_cash_limit`، صفر = بلا سقف): تجاوزه يمنع **طلبات الدفع عند الاستلام فقط**.
- **فى التطبيق:** «كسبتَ اليوم» · «النقد بحوزتك» · «الصافى» — **لكل مطعم على حدة**.
- ⚠️ **كل قيد بمفتاح idempotency** (`order_id + type`) — رفع مكرر من الطابور لا يقيّد مرتين.

---

## 🚨 محرك النداء (Dispatch)

```
١) الطلب يُؤكَّد (أو يصير جاهزاً — حسب driver_dispatch_timing)
٢) DispatchPolicy يجمع المرشّحين: online · مرتبطون بالفرع · أحرار · بلا عرض معلّق ·
   ضمن نصف القطر · وتحت السقف النقدى إن كان الطلب نقدياً
٣) يرتّبهم حسب وضع الربط  →  عرض للأول بمهلة ٤٥ث
٤) ينادى بطريقتين معاً:
      • FCM رسالة بيانات عالية الأولوية → توقظ التطبيق حتى فى Doze
      • الخدمة الأمامية (تعمل أصلاً للموقع) تستطلع كل ١٥ث → شبكة أمان
٥) التطبيق: شاشة كاملة + صوت متواصل + اهتزاز + عدّاد
٦) ACK فورى → لا ACK ١٥ث ⇒ إعادة إرسال · ٤٥ث ⇒ المرشّح التالى
٧) قبول ⇒ إسناد ذرّى · رفض/انتهاء ⇒ التالى · نفدوا ⇒ يبقى معروضاً + إشعار المطعم
```

### «الأقرب للفرع»
```
«متاح» ⇒ موقع كل ٥–١٠ث → Redis GEOADD driver:loc:{companyId}
عند الطلب → GEOSEARCH حول إحداثيات الفرع بنصف قطر branches.driver_search_radius_km (٥)
          ⇒ قائمة مرتّبة بالمسافة فى أجزاء من الملّى ثانية، بصفر تكلفة
```
- المسافة **هوائية** فى المرحلة ١؛ المرحلة ٢ تستبدلها بزمن وصول حقيقى **للأقرب خمسة فقط**.
- **الترتيب = المسافة + نسبة القبول**؛ ⚠️ معامل نسبة القبول يجب ألّا يتجاوز أثر **كيلومتر واحد** وإلا تجاوز البعيدُ القريبَ وشكا الطرفان. التعادل للأطول انتظاراً.
- ⚠️ **مناطق التوصيل لا تحدّد من يُنادى.** `DeliveryZone` تحدّد **رسوم العميل وأجر السائق** فقط؛ النداء على بُعد السائق **عن الفرع** لأنه يذهب للفرع أولاً.

### 🔴 القفل — ما يجعل «متاح لكل مطاعمه» آمناً
```
عرض معلّق واحد لكل سائق فى كل لحظة
+ سقف طلب واحد ⇒ السائق إمّا حرّ (مرشّح لكل مطاعمه) أو مشغول (مرشّح لا أحد)
+ الإسناد ذرّى:
    UPDATE orders SET delivery_driver_id=?, driver_claimed_at=NOW()
    WHERE id=? AND delivery_driver_id IS NULL      ← واحد يفوز، والخاسر يرى «سُبقت إليه»
```
مطعمان يعرضان فى نفس الثانية ⇒ أول قفل يفوز والثانى ينتقل للتالى. **فلا حاجة لمحرك أولويات المرحلة ٤.**
- **إيجار لا إسناد أبدى:** من قَبِل ولم يتحرّك نحو الفرع خلال `driver_pickup_grace_minutes` (٥) يعود الطلب للطابور.

### 🔴 سطح واحد للقرار
`App\Services\Delivery\DispatchPolicy` وحده يقرأ الإعدادات ويقرّر — لا `if ($branch->driver_assignment_mode === …)` فى أى كنترولر. و`DriverAssigner` هو **المكان الوحيد** الذى يكتب `orders.delivery_driver_id` (يوحّد اليدوى والتلقائى والالتقاط ويكتب الحدث).

---

## ⚙️ إعدادات التوصيل لكل فرع (`BranchForm` ← قسم «التوصيل بالسائقين»)

### `driver_assignment_mode` (الافتراضى `manual`)
| القيمة | السلوك |
|---|---|
| `manual` | المطعم يختار السائق لكل طلب |
| `auto_nearest` | أقرب سائق متاح (Redis GEO) — التعادل للأقل حملاً |
| `auto_least_loaded` | الأقل طلبات اليوم؛ التعادل **للأكثر توصيلاً فى يوم العمل** |
| `self_claim` | يظهر لكل المتاحين وأول من يلتقطه يفوز |

الوضعان التلقائيان **يسقطان على `self_claim`** إن لم يوجد مرشّح، ولا يُلغى الطلب أبداً.

### `driver_dispatch_timing` (الافتراضى `on_ready`)
| القيمة | متى |
|---|---|
| `on_new_order` | فور دخول الطلب (قبل قبول المطبخ) |
| `on_ready` | عند ضغط المطبخ «جاهز» |
| `by_item_prep_time` | بعد **مجموع** `preparation_time` من `confirmed_at` (مطبخ تسلسلى) |
| `by_max_item_prep_time` | بعد **أعلى** `preparation_time` من `confirmed_at` − `driver_dispatch_lead_minutes` |

- الحسابان من **`confirmed_at`** لا `created_at`، و**بتوقيت الشركة**. الصنف بلا زمن يُعدّ بـ`driver_default_prep_minutes`.
- job مؤجَّل **يعيد التحقق عند التنفيذ**؛ ولو صار `Ready` قبل الموعد ⇒ يُرسَل فوراً (الجاهزية الفعلية تفوز على التقدير).
- الطلبات المجدولة تُحسب من `scheduled_for`.
- **`driver_show_before_ready`** (true): يرى الطلب ويتحرّك، وزر «استلمت» **معطّل حتى `Ready`** مع عدّاد «جاهز خلال ~N د».

### باقى الإعدادات
`driver_search_radius_km` (5) · `driver_accept_timeout_seconds` (45) · `driver_pickup_grace_minutes` (5) · `driver_dispatch_lead_minutes` (5) · `driver_default_prep_minutes` (10) · `driver_max_active_orders` (**1**) · `driver_cash_limit` (0) · `driver_fee_by_zone` (json).

---

## 🔔 الإشعارات — FCM مباشر ومعمارية «صفر إشعار ضائع»

الناقل **ليس** ما يمنع الضياع — بل التحقّق من الوصول:
1. **FCM رسالة بيانات عالية الأولوية** (`priority: high`, data-only) توقظ التطبيق فى Doze.
2. **استطلاع كل ١٥ث** من الخدمة الأمامية العاملة أصلاً (مجانى عملياً).
3. **🔑 ACK:** التطبيق يُقرّ فور العرض. لا إقرار ١٥ث ⇒ إعادة إرسال · ٤٥ث ⇒ **إعادة التوزيع**. فالإشعار الضائع يصير «طلب ذهب لغيره خلال ٤٥ث» لا «طلب ضاع».

- **قناة إشعار مخصّصة:** أهمية قصوى + صوت متواصل + اهتزاز + **تجاوز عدم الإزعاج**.
- **شاشة كاملة** حين يكون التطبيق مفتوحاً. ⚠️ `USE_FULL_SCREEN_INTENT` مقيّدة فى أندرويد 14+ لغير تطبيقات الاتصال — نطلبها ونسقط على إشعار عالى الأولوية إن رُفضت.
- **مؤشّر صحة داخل التطبيق:** «الإشعارات تعمل ✅ / معطّلة ⚠️» بزر يفتح إعدادات النظام.
- الباك اند: `App\Services\Fcm\FcmService` (FCM v1 بحساب خدمة). **OneSignal يبقى كما هو للمطعم والعملاء.**

---

## 📦 التوزيع والتحديث الذاتى

```
GET /api/v1/driver/app-version → {min_version, latest_version, apk_url, sha256, notes_ar/en}
التطبيق يفحص عند الإقلاع وكل ٦ ساعات:
   إصدارى < min_version    ⇒ شاشة كاملة تمنع الاستخدام + زر تحميل
   إصدارى < latest_version ⇒ بانر يُذكّر بلا منع
   التنزيل داخل التطبيق → تحقّق sha256 → PackageInstaller
```
- صلاحية `REQUEST_INSTALL_PACKAGES`. ⚠️ **تُحذف قبل أى نشر عام على Play.**
- APK مستضاف على خادمنا بمسار محمى، والرابط للتطبيق المصادَق فقط.
- **لا يُجبَر التحديث وبيد السائق طلب قائم** — يُؤجَّل حتى التسليم (مرة واحدة).
- `driver.client` middleware يرفض إصداراً أقدم من `min_version` بـ426 + `code: app_update_required`.

---

## 🔐 المصادقة والدخول

```
١) المطعم يضيف السائق: الاسم + الجوال  →  كود دعوة + رابط واتساب
٢) السائق يُدخل جواله  →  OTP عبر Firebase Phone Auth
٣) كود الدعوة (أو ربط تلقائى إن كان جواله مدعوّاً)  →  رمز سرّى من ٤ أرقام
٤) لاحقاً: الجوال + الرمز (OTP عند تغيير الجهاز أو نسيان الرمز)
```
- الدخول يُرجِع **كل مطاعمه النشطة**، والتوكن على **`driver_id`** لا `user_id` فيرى طلبات كل مطاعمه معاً.
- **جلسة جهاز واحدة** · التوكن **٣٠ يوماً** يتجدّد بالاستخدام.
- limiter `driver-login`: 10/دقيقة لكل IP + 5/دقيقة لكل (phone+IP). **بلا قفل حساب صلب** (DoS).
- `driver.auth`: توكن → `is_active` → رابط نشط واحد على الأقل → شركة نشطة → اشتراك → بوّابة الخطة. أكواد 403: `account_inactive`, `no_active_link`, `company_inactive`, `subscription_expired`, `feature_not_in_plan`.
- **كل 4xx تحمل `code`** — التطبيق يترجم بالكود لا بنصّ الرسالة.
- ⚠️ Firebase Phone Auth يحتاج **خطة Blaze** + بصمة SHA-1 لكل نكهة/نوع بناء.

---

## 🚚 دورة الرحلة

```
[غير متاح] ──► [متاح] ──(عرض ٤٥ث أو التقاط)──► [مُعيَّن]
                                                  │ «وصلت للفرع» (محلى فقط)
                                                  ▼
                       [استلمت] = Ready → OutForDelivery   (معطّل حتى Ready)
                                                  │ ملاحة خارجية · اتصال · واتساب
                                                  ▼
                       [سلّمت] = OutForDelivery → Delivered (+ النقد المحصَّل)
                                                  │
                                         [مشكلة] → issue (بلا تغيير حالة)
```
- **لا يستطيع السائق إلغاء طلب** — يسجّل مشكلة فقط. الإلغاء باب واحد من المطعم عبر `transitionTo(Cancelled)` بسبب.
- **الإلغاء أثناء الرحلة** ⇒ بثّ + إشعار + شاشة كاملة «أُلغى الطلب — أعِده للفرع».
- **بابان لـ«خرج للتوصيل»** (المطعم والسائق) **عمداً** — جوال السائق قد يموت.
- **إعادة تعيين أثناء الرحلة:** تغيير `delivery_driver_id` بلا تغيير حالة + إشعار «سُحب الطلب منك».
- **التسليم:** «حصّل: X ر.س» بخط كبير + المبلغ مملوءاً وقابلاً للتعديل **بسبب إلزامى** + تأكيد بضغطة مطوّلة.
- **الملاحة:** `google.navigation:q=lat,lng` (وWaze) — **لا ملاحة داخل التطبيق**.
- 🔴 **قبل القبول:** الحىّ + المسافة + المبلغ فقط. **العنوان الكامل وجوال العميل بعد القبول.**
- يرى **أصناف الطلب** (ملخّص بلا أسعار) و**ملاحظة العميل** و**زر اتصال بالفرع**.

---

## 📍 الموقع فى الخلفية — أخطر جزء

- **ForegroundService** بإشعار دائم أثناء «متاح» (نوع `location`).
- ترتيب الصلاحيات: `ACCESS_FINE_LOCATION` → شاشة شرح → `ACCESS_BACKGROUND_LOCATION` → `POST_NOTIFICATIONS`. **لا تطلبها دفعة واحدة.**
- التردد: متحرك 5–10ث · متوقف 30ث · متاح بلا طلب 60ث · **غير متاح = لا تتبّع إطلاقاً**.
- الإرسال **مجمَّع** كل 15ث؛ آخر موقع فقط على الخادم، و`last_seen_at` فى MySQL **مرة/دقيقة على الأكثر**.
- **Heartbeat:** انقطاع 3 دقائق ⇒ الخادم يُطفئ التوفّر ويُشعِره. والتطبيق يعرض «الاتصال منقطع» فور فشل الإرسال.
- **🔴 رفض «طوال الوقت»:** يعمل **مع لافتة حمراء دائمة** + زر يفتح الإعدادات. ⚠️ **صمّم كل شىء ليعمل بالموقع الأمامى فقط** — يحميك أيضاً عند النشر العام لاحقاً.
- **قتلة البطارية** (Xiaomi/Oppo/Vivo/Huawei/HONOR/Samsung): شاشة تكشف المُصنِّع وتعرض **دليلاً مصوّراً** + `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`. **جزء من المنتج لا تفصيل.**
- **قياس:** نسبة المتاحين بلا موقع 3 دقائق = مؤشر «قتل التطبيق».
- **مسار الرحلة:** نقطة/دقيقة تُحفظ مضغوطة وتُحذف بعد 30 يوماً (يحسم نزاع «لم يصل»).
- ⚠️ **المحاكى لا يثبت شيئاً هنا** — التحقق فى الجيب ساعة.

---

## 🔌 عقد الـAPI — `/api/v1/driver/*`

```
POST   driver/auth/request-otp   {phone}
POST   driver/auth/verify-otp    {phone, firebase_token} → {driver, restaurants[], needs_pin}
POST   driver/auth/set-pin       {pin}
POST   driver/auth/login         {phone, pin, device_name, app_version}
                                 → {token, driver, restaurants[{company, branches[], fleet}]}
POST   driver/auth/redeem-invite {code}
POST   driver/auth/logout  ·  GET driver/me  ·  POST driver/broadcasting/auth
GET    driver/app-version        (عام)

PATCH  driver/availability       {online}
POST   driver/location           {points:[{lat,lng,accuracy,speed,heading,recorded_at}]}

GET    driver/orders/available · mine · {id} · history?date=   (٧ أيام)
POST   driver/orders/{id}/ack          ← إقرار استلام العرض
POST   driver/orders/{id}/accept · /decline {reason} · /claim · /release
POST   driver/orders/{id}/picked-up
POST   driver/orders/{id}/delivered    {cash_collected?, note?, occurred_at}
POST   driver/orders/{id}/issue        {code, note?}
POST   driver/orders/{id}/breadcrumbs  {points[]}

GET    driver/ledger?company_id=  → {earned_today, cash_on_hand, net, entries[], cash_limit}
GET    driver/ledger/settlements
```

### شكل الطلب (`DriverOrderResource`)
`id, order_number, status, delivery_method, payment_method, payment_status, total, cash_to_collect, driver_fee, company{name, logo}, branch{id,name,address,lat,lng,phone}, zone{name}, distance_km, customer{name, phone}*, delivery_address{text, lat, lng, notes}*, items[{name, quantity}], notes, expected_ready_at, ready_at, offer{expires_at}`
**`*` لا تُرسَل قبل القبول إطلاقاً.**
⚠️ **لا تُرسل للسائق:** بريد العميل، الكوبونات، الخصومات، تفاصيل الدفع الإلكترونى، ملاحظات الموظفين.

### الفورى (Reverb)
`private-driver.{driverId}`: `DriverOrderOffered` · `DriverOrderAssigned` · `DriverOrderUpdated` · `DriverOrderTaken` · `DriverForceOffline`. واستطلاع كل 20ث كشبكة أمان.

### 🔴 قواعد الخادم
- **كل أمر رحلة يحمل `Idempotency-Key`** — التكرار يعيد نفس الرد 200.
- **`occurred_at` من الجهاز** مقبول ومقصوص بين (الآن − ٦ ساعات) والآن؛ يُخزَّن الاثنان.
- **كل تغيير حالة عبر `Order::transitionTo()` حصراً** — لا بثّ يدوى بعده.
- **عزل المستأجرين:** كل استعلام مُقيَّد بشركات روابط السائق النشطة. اختبار العزل إلزامى.
- الحسابات الزمنية **بتوقيت الشركة**.

### ما يتغيّر فى الباك اند
```
enums:       DriverAssignmentMode · DriverDispatchTiming · DriverLedgerEntryType ·
             OrderDeliveryEventType · DriverOfferOutcome
models:      Driver · DriverRestaurantLink · DriverToken · DriverOffer · Fleet ·
             DriverLedgerEntry · DriverSettlement · OrderDeliveryEvent · TripBreadcrumb
services:    DriverAuthService · DriverLocationService (Redis) · Delivery\DispatchPolicy ·
             Delivery\DriverAssigner · Delivery\DriverLedgerService · Fcm\FcmService ·
             DriverInviteService
jobs:        DispatchOrderToDrivers (مؤجَّل) · ExpireDriverOffer · ResendDriverOffer ·
             SweepStaleDrivers · SweepPickupLease · PurgeOldBreadcrumbs · NotifyRestaurantNoDriver
controllers: Api\V1\Driver\{Auth, Availability, Location, Order, Ledger, AppVersion, Broadcasting}
panel:       قسم «التوصيل بالسائقين» فى BranchForm · أعمدة (متصل/آخر ظهور/رصيد) فى
             DeliveryDriversTable · صفحة «تسوية السائقين» · خط زمنى التوصيل فى OrderInfolist ·
             لافتة «لا سائق متاح» · زر «أجر رحلة فاشلة»
plan:        🔴 PlanFeature::DeliveryManagement — New-Feature Checklist كاملاً
routes:      Route::prefix('driver')   ⚠️ route:cache إلزامى بعد النشر
lang:        lang/{ar,en}/driver.php
tests:       tests/Feature/Driver/* — المصادقة، تعدد المطاعم، العزل، الأوضاع الأربعة،
             التوقيتات الأربعة، القفل الذرّى، ACK وإعادة التوزيع، الدفتر والمقاصّة،
             السقف النقدى، idempotency، بوّابة الخطة
```

---

## 🧱 بنية المشروع والمكتبات

```
driver-android-saas-whatsapp-qrmenu/
├── CLAUDE.md · PLAN.md · openapi/driver.v1.yaml · scripts/verify.sh
├── build-logic/                  ← convention plugins من POS (pos→driver)
├── app/                          ← نكهتا meniura/taaj + driver_brand_colors.xml لكل نكهة
├── core/{common,model,network,database,datastore,designsystem,ui,location,sync,notifications,updater}
└── feature/{auth,availability,trip,ledger,history,settings,onboarding}
```

### الإصدارات (طابق POS)
```
agp 8.11.2 · kotlin 2.0.21 · ksp 2.0.21-1.0.28 · compose-bom 2024.12.01 · hilt 2.52
room 2.6.1 · retrofit 2.11.0 · okhttp 5.0.0-alpha.14 · kotlinx-serialization 1.7.3
coroutines 1.9.0 · workmanager 2.9.1 · datastore 1.1.1 · sentry-android 8.16.0
+ firebase-bom (auth + messaging) · play-services-location · accompanist-permissions
+ coil · libphonenumber · spotless/ktlint
```
⚠️ **لا ترفع `compose-bom`** (الأحدث يتطلّب AGP 9).

### نستخدمها / لا نستخدمها
| نستخدمها | لا نستخدمها |
|---|---|
| FusedLocation · Firebase Auth+Messaging · accompanist-permissions · Coil · Retrofit/Room/Hilt/WorkManager (منسوخة) · libphonenumber · Sentry · Spotless · **Static Maps API (صورة)** | مكتبة تتبّع خلفى مدفوعة (~$300؛ قيمتها كانت لـFlutter) · **Maps SDK كامل** · Directions/Routes · OneSignal (لهذا التطبيق) · Lottie · قوالب «تطبيق سائق» · مكتبة geo (haversine = ١٠ أسطر) · **Play In-App Updates** (لا تعمل مع APK مُثبَّت يدوياً) |

### الأنماط المنسوخة من POS
`Flavor.kt` + convention plugins · `NetworkModule` + interceptors (**`DnsRetryInterceptor` مهم على HONOR/Huawei**) · `TokenStore` (EncryptedSharedPreferences) · `LocaleManager`+`AppLocaleStore` · `OrderUploadWorker`+`SyncScheduler`+`ConnectivityObserver` → `DriverActionUploadWorker` · `BackendContractMirrorTest` · **نظام التصميم** (انظر قسمه) · المهارات السبع فى `.claude/skills/`.

---

## ✅ بروتوكول التحقق بعد كل تعديل (على الجهاز — لا على GitHub)

> لا `commit` بلا هذا كاملاً. `scripts/verify.sh` يشغّله دفعة واحدة، ونتيجته تُلصَق فى رسالة التسليم.

### 0) البيئة
```bash
export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"      # لا JDK فى PATH
ADB="$LOCALAPPDATA/Android/Sdk/platform-tools/adb.exe"               # adb ليس فى PATH
"$ADB" devices
```
- ⚠️ **بلا `JAVA_HOME` يخرج Gradle بلا سطر `BUILD`** — ابحث عن `BUILD SUCCESSFUL` حرفياً.
- الباك اند محلياً: `php artisan serve` + `"$ADB" reverse tcp:8000 tcp:8000` + `driver.debugBaseUrl=http://127.0.0.1:8000`. أطفئه و`adb reverse --remove-all` بعدها.
- 🔴 **جهاز الاختبار: Samsung S25 (هاتف).** الجهاز الآخر HONOR NDL-W09 **تابلت** لا يمثّل تطبيق سائق. وقبل البيتا الميدانية **أضف هاتفاً رخيصاً** — التطبيق يموت على الرخيص لا على الرائد.

### 1) البناء
```bash
./gradlew :app:assembleMeniuraDebug :app:assembleTaajDebug
./gradlew :app:hiltJavaCompileMeniuraDebug        # بعد أى حقن جديد
```
### 2) الاختبارات
```bash
./gradlew testMeniuraDebugUnitTest
./gradlew :core:database:testMeniuraDebugUnitTest   # ترحيلات Room — تُنفَّذ فعلياً
./gradlew :core:network:testMeniuraDebugUnitTest    # مرآة العقد
```
- **🔴 ترحيل Room يُتحقَّق بالتشغيل لا بالقراءة** (`MigrationTestHelper` على مخطط الإصدار السابق).
- أى تغيير فى `openapi/driver.v1.yaml` ⇒ إعادة توليد `core/network/src/test/resources/contract/driver.v1.json`.
### 3) الباك اند (إن لُمس)
```bash
php artisan test --compact --filter=Driver
vendor/bin/pint app tests database routes lang config --dirty    # مرّر المسارات صراحةً
```
### 4) الجهاز الحقيقى
```bash
"$ADB" install -r app/build/outputs/apk/meniura/debug/app-meniura-debug.apk
"$ADB" logcat -c
"$ADB" shell am start -n app.qrmenu.driver.meniura/app.qrmenu.driver.MainActivity
sleep 6
"$ADB" logcat -d | grep -E "FATAL|AndroidRuntime|ANR" && echo "❌ CRASH" || echo "✅ no crash"
"$ADB" exec-out screencap -p > _screenshots/$(date +%Y%m%d-%H%M%S)-launch.png
```
- **سيناريو الدخان:** دخول بالجوال → متاح → طلب حقيقى → عرض ← ACK ← قبول → استلمت → سلّمت (+نقد) → التحقق من `delivered` و`order_status_history` و`order_delivery_events` و**قيود الدفتر**.
- **لقطة لكل شاشة تغيّرت** فى `_screenshots/`، **وفى الوضعين الفاتح والداكن**.
- ⚠️ **لأى تغيير يمسّ الموقع/الخلفية: اختبار ميدانى** — «متاح» والجهاز فى الجيب 30–60 دقيقة ثم فحص `last_seen_at`. لا بديل.
- ⚠️ على الجهاز: انقر بموقع النص من `uiautomator` لا بإحداثيات ثابتة.
### 5) بعد النجاح فقط
حدّث `CLAUDE.md` فى نفس التغيير → `git add` للملفات المقصودة (لا `-A` بلا مراجعة) → `commit` على `main` → أخبر صاحب المشروع (الاختبارات + ما ثُبِّت + اللقطات) → `push` بعد موافقته.

---

## 🔐 الأمان والخصوصية
- التوكن مُجزّأ `sha256` فى القاعدة و`EncryptedSharedPreferences` على الجهاز. **401 = خروج كامل**.
- **لا PII فى السجلات ولا Sentry** — `beforeSend` يمسحها.
- الموقع يُجمع **فقط أثناء «متاح»**، ويُذكر فى شاشة الصلاحية وسياسة الخصوصية.
- **جوال العميل وعنوانه: بعد القبول فقط**، ويختفيان من الواجهة والكاش بعد التسليم.
- مفتاح الخرائط مقيّد ببصمة SHA-1 لكل نكهة + ميزانية وحصص يومية.
- توقيع الإصدار: `DRIVER_RELEASE_*` من `~/.gradle/gradle.properties` — لا يُلتزم أبداً.
- 🔴 **`REQUEST_INSTALL_PACKAGES` تُحذف قبل أى نشر عام على Play.**

---

## 🗺️ المراحل

### المرحلة ٠ — التأسيس (يوم–يومان)
`git init` ✅ · `.gitignore` ✅ · نسخ `build-logic`+`settings.gradle.kts`+`libs.versions.toml` وإعادة التسمية · الوحدات الفارغة · `core/network`+`core/datastore`+**`core/designsystem` (بانحرافاته الأربعة)** · `.claude/skills/` · `scripts/verify.sh` · شاشة اللغة + شاشة الدخول.
**القبول:** `verify.sh` أخضر · التطبيق يفتح على S25 بالنكهتين · اللغات الخمس بأرقام لاتينية · **الوضعان الفاتح والداكن** · تكبير خط النظام 200٪ بلا كسر.

### المرحلة ١ — MVP (٨–١٠ أسابيع) ← الأولوية
| الأسبوع | الباك اند | التطبيق |
|---|---|---|
| ١ | `drivers`+`links`+`fleets`+`driver_tokens` · OTP/PIN/login/invite | الدخول بالجوال + OTP + الرمز + قائمة المطاعم |
| ٢ | availability + location (Redis) + sweep + بثّ | «متاح» + ForegroundService + الإرسال المجمَّع + Heartbeat + شاشة البطارية |
| ٣ | إعدادات الفرع + `DispatchPolicy` + التوقيتات الأربعة | قائمتا الطلبات + بطاقة الطلب + عدّاد «جاهز خلال» + الطابور المحلى |
| **٣ ✅** | **أول نسخة تُجرَّب على جوالك: دخول + متاح + طلبات حقيقية + دورة رحلة** | |
| ٤ | `DriverAssigner` + العرض والقفل + ACK + إعادة التوزيع + `FcmService` | شاشة العرض الكاملة بعدّاد وصوت + قبول/رفض + الالتقاط |
| ٥ | picked-up/delivered/issue + الإشعارات + الإلغاء + breadcrumbs | شاشة الرحلة كاملة + التسليم بالنقد + المشاكل |
| ٦ | **الدفتر والمقاصّة** + التسوية + السقف النقدى + `driver_fee` لقطة | شاشة الدفتر لكل مطعم |
| ٧ | `PlanFeature::DeliveryManagement` + Checklist · لوحة الشركة | السجلّ (٧ أيام) + الإعدادات + التدريب + Sentry |
| ٨ | المُحدِّث الذاتى + `driver.client` | المُحدِّث داخل التطبيق + البانر + الشاشة المانعة |
| ٩–١٠ | تصليب: العزل، idempotency، الأداء، `route:cache`، الخصوصية | **بيتا مغلقة: 3–5 سائقين على هاتفين رخيصين أسبوعين** |

**القبول النهائى:** دعوة سائق → دخول بجواله → متاح → طلب يُؤكَّد → يصل بالوقت والطريقة اللذين يحددهما الفرع (≤5ث) → ACK مسجَّل → قبول → استلمت (بعد Ready) → العميل يرى «فى الطريق» → سلّمت (+نقد) → `delivered` + قيود الدفتر صحيحة + التسوية تُصفّر الرصيد → الجهاز فى الجيب 60 دقيقة بلا انقطاع.

**ليس فى المرحلة ١ عمداً:** ETA عبر API · موجات متعددة · تجميع · OTP تسليم · صورة إثبات · تقييم · خريطة حيّة للمطعم · تتبّع العميل للسائق · كشف أجور · iOS.

### المرحلة ٢ (٤–٦ أسابيع)
ETA حقيقى للأقرب خمسة · موجات (الأفضل 20ث → أفضل 3 → الكل) · تجميع طلبين من نفس الفرع خلال 5د ووجهتين ≤2كم (**لا ثالث**) · خريطة حيّة للمطعم · تتبّع السائق للعميل عبر `lookup_token`.

### المرحلة ٣ (٣–٤ أسابيع)
كشف أجور أسبوعى **للمطعم وحده** · OTP تسليم عبر واتساب + صورة اختيارية · تقييم السائق ومؤشراته · «عميل لا يرد» بمؤقّت ومحاولتين.

### المرحلة ٤ (٦–٩ أشهر، قرار منفصل)
بعتبة `no_driver_available` + رأى قانونى (ترخيص الهيئة العامة للنقل، وضع السائق المستقل، التأمين، ضريبة الخدمة) · انضمام عام بوثائق واعتماد · دفتر وتسويات على مستوى المنصة · حوافز · كونسول عمليات.

---

## 📋 ما يحتاجه صاحب المشروع

| # | | الحالة |
|---|---|---|
| ١ | هاتف Samsung S25 | ✅ |
| ٢ | حساب Google Play | ✅ (لن يُستخدم فى المرحلة ١) |
| ٣ | **مشروع Firebase** (Phone Auth + FCM) + **خطة Blaze** | ⏳ |
| ٤ | **مفتاح توقيع (keystore)** + نسختان احتياطيتان | ⏳ 🔴 فقدانه = لا تحديث أبداً |
| ٥ | مفتاح خرائط جوجل مقيّد + تنبيه ميزانية | ⏳ |
| ٦ | مشروع Sentry + DSN | ⏳ |
| ٧ | فقرة الموقع فى سياسة الخصوصية لكل نكهة | ⏳ |
| ٨ | أيقونة (شعار + شارة) لكل نكهة | ⏳ |
| ٩ | رقم جوال حقيقى لاختبار OTP | ⏳ |
| ١٠ | فرع تجريبى بإحداثيات + مناطق توصيل برسوم + عنوانا عميلين بإحداثيات | ⏳ يُضاف لـ`NewdemoSeeder` |
| ١١ | هاتف رخيص (Xiaomi/Samsung A) | ⏳ قبل البيتا |
| ١٢ | مراجع بشرى للترجمات الخمس | ⏳ قبل الإطلاق |
| ١٣ | **أول مطعم مستخدم** (عدد سائقيه، حجم طلباته) | ❓ لم يُحدَّد |

---

## ⚠️ مخاطر مفتوحة

| # | الخطر | الخطة |
|---|---|---|
| أ | **أجهزة بلا خدمات جوجل** (هواوى حديثة) | غير مدعومة + رسالة. S25 وHONOR الحالى بهما GMS ✅ |
| ب | **قتل التطبيق على أجهزة رخيصة** | دليل البطارية + heartbeat + مؤشر خادمى + بيتا على جهاز رخيص |
| ج | **`REQUEST_INSTALL_PACKAGES` عند النشر العام** | تُحذف ويُستبدل بـPlay In-App Updates |
| د | **صلاحية الموقع الخلفى عند النشر العام** | التطبيق مصمَّم للعمل بالموقع الأمامى فقط |
| هـ | **تفرّد البريد فى `users`** لسائق بمطعمين | الهوية على الجوال؛ `users.email` يُولَّد اشتقاقياً إن لزم |
| و | **Firebase Phone Auth على APK مُثبَّت يدوياً** | Play Integrity قد يفشل فيسقط على reCAPTCHA — **يُختبر فى الأسبوع الأول** |
| ز | **ترخيص النقل** | المرحلة ٤ فقط — لا علاقة له بالمرحلة ١ (المال لا يمرّ بنا) |

---

## 🔢 ترقيم الإصدارات (يعتمد عليه المُحدِّث — جمّده الآن)

```
versionName = MAJOR.MINOR.PATCH          مثال 1.2.3
versionCode = MAJOR*10000 + MINOR*100 + PATCH   مثال 10203
```
- 🔴 **المقارنة على `versionCode` (عدد صحيح) لا على `versionName` (نصّ).** مقارنة نصّية تجعل `1.10.0` **أقدم** من `1.9.0` فيُمنَع سائق يحمل أحدث إصدار.
- الخادم يُرجِع `min_version_code` و`latest_version_code` **بالإضافة** إلى الاسمين (الاسم للعرض فقط).
- `versionCode` **لا يتراجع أبداً** ولو رجعنا عن ميزة.
- نفس الرقم للنكهتين (النكهة تفرّقها `applicationId`).

---

## 🚦 أكواد الأخطاء — القائمة المُجمَّدة

🔴 التطبيق **يترجم بالكود لا بنصّ الرسالة** (الخادم يدعم ar/en فقط، والسائق قد يكون أردياً). أى كود جديد يُضاف هنا **وفى الترجمات الخمس معاً**، وإلا ظهر للسائق نصّ إنجليزى خام.

| المجال | الأكواد |
|---|---|
| المصادقة | `invalid_credentials` · `otp_invalid` · `otp_expired` · `too_many_attempts` · `invalid_invite` · `invite_already_used` |
| الحساب | `account_inactive` · `no_active_link` · `company_inactive` · `subscription_expired` · `feature_not_in_plan` |
| الإصدار | `app_update_required` |
| التوفّر | `has_active_trip` · `driver_offline` · `location_permission_required` |
| الطلبات | `already_claimed` · `offer_expired` · `too_many_active_orders` · `order_not_ready` · `order_cancelled` · `not_your_order` · `cash_limit_exceeded` · `branch_closed` |
| عام | `validation_failed` · `rate_limited` · `server_error` · `network_unavailable` (محلى) |

- **كود غير معروف للتطبيق ⇒ رسالة عامة مترجمة + تسجيل فى Sentry** (لا تعرض نصّ الخادم مباشرة).

---

## 🗺️ خريطة الشاشات (١٨ شاشة — نطاق المرحلة ١)

| # | الشاشة | الوحدة | ملاحظة |
|---|---|---|---|
| 1 | اختيار اللغة | onboarding | ✅ **مبنية** — أول تشغيل، بلا افتراض، معاينة حيّة |
| 2 | الدخول (جوال) | auth | ✅ **مبنية** — كود الدولة يساراً ومكتشَف · كلمة مرور بزر عين · «تذكّر رقمى» · «نسيت» · «أول مرة؟» · الإصدار أسفل |
| 3 | OTP | auth | ✅ **مبنية** — **٦ خانات** مكشوفة بلا عين · عدّاد إعادة إرسال · تغيير الرقم |
| 4 | اختيار كلمة المرور | auth | ✅ **مبنية** — حقلان بعين مشتركة + تأكيد |
| 5 | إدخال كود الدعوة | auth | أو ربط تلقائى بالجوال |
| 6 | التدريب | onboarding | 4–5 شرائح |
| 7 | الصلاحيات | onboarding | الموقع ثم الإشعارات، بالترتيب |
| 8 | **الرئيسية** | availability | ✅ **مبنية** — متاح/غير متاح + سبب «لا طلبات» مسمّى + فروعى بأسمائها ومسافاتها |
| 9 | **العرض (شاشة كاملة)** | trip | ✅ **مبنية** — عدّاد **إلى لحظة الخادم** لا ٤٥ث من فتح الشاشة · ACK فور الظهور · قبول/رفض بـ٦٤dp · بلا أى حقل يحمل عميلاً أو عنواناً. ⚠️ الصوت والاهتزاز من `:core:notifications` ويُوقفهما `onAccepted`/`onResolved` |
| 10 | تفاصيل الطلب / الرحلة | trip | ⏳ القائمتان والبطاقة مبنيتان فى `:feature:orders`؛ شاشة الرحلة نفسها أسبوع ٥ |
| 11 | التسليم (النقد) | trip | مبلغ + تأكيد مطوّل |
| 12 | تسجيل مشكلة | trip | ٥ أسباب + ملاحظة |
| 13 | **الدفتر** | wallet | ✅ **مبنية** — تبويب «المحفظة»: الأرقام الثلاثة + الدفتر + التسويات، لكل مطعم على حدة |
| 14 | السجلّ | history | ٧ أيام |
| 15 | الإعدادات | account | ✅ **مبنية داخل «حسابى»** — اللغة · حجم التطبيق بمعاينة حيّة · البطارية · خروج |
| 16 | دليل البطارية | settings | حسب المُصنِّع، مصوَّر |
| 18b | **مركز الإشعارات** | notifications | ✅ **مبنى** — يُفتح من الجرس؛ حالته الفارغة تقول «سنخبرك هنا لحظة عرض أى طلب» لا «لا توجد إشعارات» |
| 17 | **التحديث الإجبارى** | updater | تمنع الاستخدام |
| 18 | جهاز غير مدعوم | onboarding | بلا خدمات جوجل |

**التنقّل:** `Navigation Compose` بمسارات type-safe (`@Serializable`). كل شاشة جديدة تُسجَّل هنا **فى نفس التغيير**.

---

## 🔤 مسرد المصطلحات (يمنع تضارب الترجمة بين الشاشات)

🔴 استخدم هذه الألفاظ حرفياً — ولا تخترع مرادفاً فى شاشة أخرى.

| المفهوم | عربى | إنجليزى | ur / bn / hi |
|---|---|---|---|
| online | **متاح** | Available | ⏳ يُملأ ويُراجَع |
| offline | **غير متاح** | Offline | ⏳ |
| offer | **عرض** | Offer | ⏳ |
| accept | **قبول** | Accept | ⏳ |
| picked up | **استلمت الطلب** | Picked up | ⏳ |
| delivered | **سلّمت** | Delivered | ⏳ |
| cash on hand | **النقد بحوزتك** | Cash on hand | ⏳ |
| earnings | **ما كسبته** | Earnings | ⏳ |
| settlement | **تسوية** | Settlement | ⏳ |
| issue | **مشكلة** | Issue | ⏳ |

⚠️ **الثلاث لغات الأخيرة يكتبها الوكيل ثم يراجعها ناطق أصلى** — لا تُطلَق بلا مراجعة.

---

## 📊 ما نقيسه

| المقياس | لماذا |
|---|---|
| **`no_driver_available`** (معدّل · بالساعة · بالحىّ) | 🔑 **خطة عمل المرحلة ٤ وعتبتها** |
| زمن العرض → ACK | يكشف عطل الإشعارات قبل أن يشتكى أحد |
| نسبة قبول كل سائق | تدخل فى الترتيب |
| الأزمنة: الإرسال→القبول · القبول→الفرع · الاستلام→التسليم | جودة الخدمة |
| **نسبة قتل التطبيق** (متاح بلا موقع 3 دقائق) | 🔴 أهم مؤشر صحة تقنى |
| الرحلات الفاشلة وأسبابها | تشغيلى |
| النقد المستحق على كل سائق + تأخّر التسوية | مالى |

---

## ⚡ ميزانية الأداء وإمكانية الوصول

| | الهدف |
|---|---|
| الإقلاع البارد | < ٢ث على S25 · < ٤ث على جهاز رخيص |
| ظهور شاشة العرض من لحظة FCM | **< ١ث** |
| استهلاك البطارية أثناء «متاح» | **< ٨٪/ساعة** — يُقاس ميدانياً لا يُفترَض |
| حجم APK | < ٢٥ ميجا |
| TalkBack | كل زر إجراء له `contentDescription` |
| تكبير خط النظام | يعمل حتى **٢٠٠٪** بلا كسر تخطيط |
| التباين | ≥ 4.5:1 (يحرسه `AccentPalette`) |

---

## 🧨 الحالات الحدّية (احسمها فى الكود لا وقت الحادثة)

| الحالة | السلوك |
|---|---|
| السائق يقفل التطبيق أثناء رحلة | الخدمة الأمامية تستأنف، والرحلة تُستعاد من Room + الخادم عند الفتح |
| **الطلب يُلغى بعد تحصيل النقد** | قيد الدفتر **يبقى** ويُعلَّم، والمطعم يسوّيه يدوياً + إشعار له |
| تغيير الجهاز | جلسة واحدة ⇒ الجديد يفوز. **الطابور يُرفع بعدوانية قبل أى خروج**، وإن تعذّر يُعرض «لديك إجراءات لم تُرسَل» |
| السائق يُعطَّل وبيده طلب | يُكمل الطلب الحالى ولا تصله عروض جديدة |
| انحراف ساعة الجهاز | `occurred_at` مقصوص بين (الآن − ٦ ساعات) والآن، والاثنان يُخزَّنان |
| الفرع يغلق والسائق فى الطريق | الرحلة تكتمل عادياً؛ الإغلاق يمنع عروضاً جديدة فقط |
| طلب بلا إحداثيات للعميل | يُعرَض بالعنوان النصّى وزر الملاحة يبحث بالنصّ + تنبيه للمطعم |

---

## 🗃️ الاحتفاظ بالبيانات

| البيان | المدة |
|---|---|
| مسار الرحلة (breadcrumbs) | **30 يوماً** ثم يُحذف |
| موقع السائق الحىّ (Redis) | آخر نقطة فقط |
| قيود الدفتر والتسويات | **دائمة** (سجلّ مالى) |
| كاش الطلبات على الجهاز | ٧ أيام |
| جوال العميل وعنوانه على الجهاز | يُمحى **بعد التسليم مباشرة** |
| أحداث التوصيل | دائمة (تدقيق) |

---

## 🛑 بروتوكول التحديث الذاتى لهذا الملف
حدّثه **فى نفس التغيير** حين: تضيف وحدة · تغيّر تبعية/إصداراً · تمسّ مخطط Room (**مع إثبات تنفيذ الترحيل**) · تضيف endpoint · تتخذ قراراً معمارياً · تصرف >30 دقيقة على مزلق · تُنهى خطوة من مرحلة.
**حين يتعارض الملف مع الكود: الكود يفوز — لكن وفّق بينهما قبل نهاية الجلسة.**

### حالة المراحل
| المرحلة | الحالة | آخر تحديث |
|---|---|---|
| ٠ التأسيس | ✅ مكتملة | 2026-09-21 |
| ١ MVP | 🟡 الأسبوع ١ (الباك اند) مكتمل — التطبيق لم يُوصَل بعد | 2026-09-21 |
| ٢ التوزيع المتقدّم | ⏳ | |
| ٣ الجودة والمال | ⏳ | |
| ٤ أسطول المنصة | ⛔ بانتظار العتبة + رأى قانونى | |

### سجلّ مخطط Room
| الإصدار | التاريخ | التغيير | تحقُّق الترحيل |
|---|---|---|---|
| 1 | — | `driver_actions_outbox` · `orders_cache` · `ledger_cache` | — |

### سجلّ القرارات
| التاريخ | القرار | السبب |
|---|---|---|
| 2026-09-21 | Kotlin/Compose أصلى، ريبو منفصل، أنماط من POS | الموقع الخلفى جوهرى ولا يوضع خلف إضافة لا نملكها · نطاق الضرر · Flutter هُجر فعلاً فى POS |
| 2026-09-21 | **السائق مستقل لا موظف** | واقع السوق السعودى — قلب قرارات الأجر والدخول وتعدد المطاعم |
| 2026-09-21 | هوية `drivers` فوق `users`، و`orders.delivery_driver_id` بلا تغيير | صفر تغيير على كود الطلبات وشاشة Filament القائمة |
| 2026-09-21 | **الإسناد بجملة `UPDATE … WHERE delivery_driver_id IS NULL`** لا بقراءة ثم كتابة تحت قفل | لا يعتمد على مستوى عزل قاعدة البيانات — نفس الفخّ (REPEATABLE READ) أنتج حجزاً مزدوجاً حقيقياً فى محرك طاولات الأماكن |
| 2026-09-21 | **«عرض معلّق واحد لكل سائق» فى فهرس فريد على عمود مولَّد** لا فى فحص PHP | الفحص يستطيع كل مستدعٍ جديد نسيانه؛ الفهرس لا |
| 2026-09-21 | **التساقط على `self_claim` عمود مخزَّن (`driver_dispatch_opened_at`)** لا اشتقاق من «لا عرض حىّ الآن» | الاشتقاق يفتح الطلب لكل السائقين فى النافذة بين موجتَى العرض — عكس ما يَعِد به الوضع التلقائى |
| 2026-09-21 | **الخاسر يرى `409 already_claimed` لا `404 not_your_order`** | «ليس طلبك» جملة كاذبة لمن كان يشاهد الطلب يعدّ تنازلياً قبل ثانية؛ الكود يسأل «هل عُرض عليه يوماً؟» قبل اختيار الرفض |
| 2026-09-21 | **`DispatchOrderToDrivers` تُجدوَل بـ`afterCommit()`** | `transitionTo` يعمل داخل معاملة فى KDS؛ بدونها يقرأ العامل الطلب قبل الـcommit ويقرّر «غير قابل للإرسال» — **ولا يُنادى أى سائق، صامتاً**، وإعادة التحقق لا تحمى لأنها هى المخدوعة |
| 2026-09-21 | **الاستطلاع كل ١٥ث هو ذراع أصيلة لا بديل مؤقت عن FCM** | يعمل بلا Firebase وبلا خدمات جوجل وعلى جوال رخيص؛ بناء الـpush أولاً كان سيعنى أن لا شىء يمكن إثباته على جهاز |
| 2026-09-21 | الدفتر بالمقاصّة فى المرحلة ١، والسائق يراه | من يعمل بالقطعة لا يستخدم تطبيقاً يخفى أجره · والمال لا يمرّ بنا |
| 2026-09-21 | متاح لكل مطاعمه + سقف طلب واحد + قفل عرض واحد | السقف يجعله إمّا حرّاً أو مشغولاً ⇒ لا حاجة لمحرك أولويات |
| 2026-09-21 | FCM مباشر + ACK (15/45ث) | Firebase داخل أصلاً لأجل OTP · والضمان من التحقق لا من الناقل |
| 2026-09-21 | مُحدِّث ذاتى بمستويين | لا رفع على Play الآن · وPlay In-App Updates لا تعمل مع APK يدوى |
| 2026-09-21 | `driver_fee` لقطة وقت الإنشاء | عرض «توصيل مجانى» كان سيصفّر أجر السائق |
| 2026-09-21 | **نظام تصميم POS مع أربعة انحرافات** (خط النظام، وضع داكن، بلا UiScale، بلا منتقى لون) | تابلت ثابت ≠ جوال سائق يعمل ليلاً وقد يحتاج تكبير الخط |
| 2026-09-21 | **IBM Plex Sans Arabic** خطاً (لا Cairo) | قياس بـ`fontTools`: الاثنان يغطّيان الأردو كاملاً، لكن IBM Plex 902 محرفاً مقابل 701 · وهو خط POS فيتوحّد تطبيقا الجهاز الواحد · وأرقامه اللاتينية حادّة والتطبيق كله أرقام. ولا خط عربى يغطّى البنغالى/الهندى — أندرويد يستبدل تلقائياً |
| 2026-09-21 | `PlanFeature::DeliveryManagement` تُباع منفصلة | قرار تجارى |
| 2026-09-21 | Redis GEO للموقع، MySQL لـ`last_seen_at` فقط | حجم الكتابة |
| 2026-09-21 | لا CI على GitHub؛ التحقق على الجهاز | قرار صاحب المشروع |
| 2026-09-21 | **`coerceInputValues` مُطفأ فى `Json`** (خلافاً لـPOS) | يُعيد كتابة `null` صريح إلى الافتراض، وحقول هذا السطح مال: `cash_to_collect = null` كانت ستصير 0.00 |
| 2026-09-21 | **`Idempotency-Key` معامل إلزامى فى التوقيع** لا interceptor بوسم اختيارى | العقد يفرضه على كل أمر رحلة؛ جعله معاملاً يجعل المترجم هو الحارس، ونسيانه يُقيَّد ضِعفين فى الدفتر |
| 2026-09-21 | **`DriverOrderDto` نوع كوتلن واحد للشكلين** offered/assigned | الضمانة البنيوية ملك الخادم؛ هنا تصل كـnullability، ونوعان متطابقان فى ١٨ حقلاً كانا سينحرفان |
| 2026-09-21 | **`DriverVersion` مصدر واحد لرقم الإصدار** فى build-logic | المُحدِّث يقارن ما يبلّغه العميل بما يختمه المثبّت؛ اختلافهما عطل صامت فى الاتجاهين |
| 2026-09-21 | `:core:network` مُضافة لـ`app` رغم أن لا شاشة تستهلكها | وحدة تُترجم وحدها لا تُثبت أن روابط Hilt تُحَلّ؛ الإضافة تجعل `hiltJavaCompile` يتحقّق منها فعلاً |
| 2026-09-21 | **أسماء اللغات ثابتة بلغتها، ليست موارد نصّية** | المنتقى لمن لا يقرأ اللغة المعروضة؛ ترجمتها تُفرغه من معناه |
| 2026-09-21 | **الضغط على لغة يعاين ولا يكتب**، والكتابة عند «متابعة» وحدها | الكتابة على الضغط تُثبّت اختياراً نصف-منجز فلا تعود الشاشة تظهر |
| 2026-09-21 | **لا `NavHost` حتى تصل وجهة ثانية حقيقية** | رسم تنقّل بوجهة واحدة يُعاد كتابته فوراً ويُخفى قرار «ما الذى يعمل أولاً» |
| 2026-09-21 | إضافة الـfeature تربط `:core:ui`/`:core:model` **إن وُجدتا فقط** | وحدتان فارغتان لإرضاء قائمة تبعيات ليستا معمارية؛ `:core:ui` تستحق وجودها مع أول شاشة تُحمّل بيانات |
| 2026-09-21 | **`GET /driver/branding` فى العقد** — الشعار والاسم من لوحة الأدمن | شعار مُجمَّع فى الـAPK يجعل تغيير الهوية يحتاج نسخة جديدة على كل جوال |
| 2026-09-21 | **رقم الجوال والرمز مثبَّتان LTR** فى كل اللغات | الرقم يُطلَب ويُطبَع من اليسار؛ انعكاسه يجعل ما يراه السائق مخالفاً لما أُملى عليه |
| 2026-09-21 | **كود الدولة من الشريحة لا من لغة الجهاز** | سائق بنغالى بشريحة سعودية: اللغة كانت ستخمّن بنغلاديش وتبنى رقماً خاطئاً صامتاً |
| 2026-09-21 | **زر إظهار الرمز السرّى** | «رمز خاطئ» و«نسيت الرمز» لا يُميَّزان وأنت ترى أربع نقاط |
| 2026-09-21 | **فشل جلب الهوية صامت** — لا لافتة ولا تعطيل | السائق جاء ليدخل؛ لافتة خطأ عن شعار تُقرأ «لا يمكنك الدخول» |
| 2026-09-21 | **🔴 صاحب المشروع: «رمز سرّى ٤ أرقام» ⇒ «كلمة مرور»** (قرار ١٧) | الرسالة تُصرف مرّة لكل جهاز لا كل وردية؛ والعقد عُدِّل معه قبل أن يُبنى الباك اند |
| 2026-09-21 | **الرمز خانات مكشوفة · كلمة المرور حقل مخفى بعين** | الشكل يتبع الاسم: خانات = شىء تنقله من رسالة، حقل مخفى = شىء تحفظه لشهور |
| 2026-09-21 | **`verify-otp` يُرجِع توكناً** (تعديل عقد) | `set-password` يحتاج جلسة؛ بدونه يُثبت السائق نفس الجوال مرّتين فى جلسة واحدة |
| 2026-09-21 | **«أول مرة؟ اطلب رمز التحقق» على شاشة الدخول** | فايربيز لا يرسل عند الدعوة — التطبيق يطلب. بدون الزر السائق الجديد مقفول بلا مخرج |
| 2026-09-21 | **لا `NavHost` للمصادقة أيضاً** (`when` على `AuthStep`) | ثلاث شاشات بمدخل وحيد ومخرج وحيد؛ رسم تنقّل كان سينثر نفس المعنى على أربعة ملفات |
| 2026-09-21 | **`DriverEligibility` سطح واحد يقرؤه الـmiddleware وlogin معاً** | إجابتان لنفس السؤال تعنى توكناً يرفضه الطلب التالى: الخطأ يظهر على شاشة غادرها السائق بدل شاشة الدخول التى يقدر يتصرّف فيها |
| 2026-09-21 | **`driver.auth` بوضعين (`full` / `account`)** | السائق لحظة إثبات جواله بلا أى رابط، و`redeem-invite` هى الطريق الوحيد لأول رابط — تبويبها بوجود رابط قفل مغلق |
| 2026-09-21 | **لون تطبيق السائق مفتاح مستقل لا سقوط على لون POS** | تعديل لون تابلت كاشير كان سيُعيد صبغ جوال كل سائق |
| 2026-09-21 | **`users.email` للسائق مشتقّ على `@drivers.invalid`** | العمود فريد على مستوى المنصة والسائق بلا بريد؛ و`.invalid` (RFC 2606) غير قابل للتوجيه فيستحيل أن يُقرأ كصندوق حقيقى |
| 2026-09-21 | **تحقّق Firebase بـ`openssl_verify` لا بمكتبة JWT** | لا مكتبة JWT مثبّتة، وإضافة تبعية تحتاج موافقة؛ والخوارزمية مثبَّتة RS256 على طرفنا ولا تُقرأ من رأس التوكن، فلا مجال لـalg confusion |

| 2026-09-21 | **مشروع Firebase لكل علامة، والملف فى `app/src/<flavour>/` ومتجاهَل فى git** | العلامتان على حسابَى جوجل مختلفين فلا يمكن مشاركة مشروع؛ وملف واحد فى `app/` كان سيجعل بناء تاج يعمل على مشروع منيورا صامتاً |
| 2026-09-21 | **نكهة بلا Firebase تُبنى وتُعلن ذلك** (`FIREBASE_CONFIGURED=false`) | الـplugin على مستوى المشروع فلا يُطبَّق لنكهة دون أخرى؛ بدون هذا كانت إضافة Firebase لعلامة تُسقط بناء الأخرى و`verify.sh` معها |
| 2026-09-21 | **مفتاح «متاح» غير تفاؤلى إطلاقاً** | سائق يراه أخضر ويضع الجوال فى جيبه قيل له إنه يكسب؛ ولو فشل النداء جلس بلا شىء |
| 2026-09-21 | **بوّابة الجلسة تقرأ التوكن مرّة عند الإقلاع لا كتدفّق حىّ** | `verify-otp` يُسجّل الدخول كى تُختار كلمة المرور، فالتوكن موجود فى **منتصف** التدفّق؛ تتبّعه يقذف السائق للرئيسية بلا كلمة مرور |
| 2026-09-21 | **`login` يفرض نفس بوّابة الـmiddleware عبر `DriverEligibility`** | العقد يوثّق الأكواد الخمسة على مسار الدخول نفسه؛ وشاشة الدخول هى المكان الوحيد الذى يتصرّف فيه السائق إزاء «حسابك موقوف» |
| 2026-09-21 | **شريط سفلى بوجهتين، وما زال `when` لا `NavHost`** | صارت هناك وجهتان حقيقيتان فالشريط مبرَّر؛ ولا شىء deep-linked بعد، فالرسم البيانى يأتى مع شاشة العرض |

| 2026-09-21 | **الأوضاع التلقائية لا تُظهر طلبها فى «المتاحة» هذا الأسبوع** | لا جدول عروض بعد؛ فتحها للكل كان يعنى أن يأخذ سائقٌ طلباً خُصّص لغيره |
| 2026-09-21 | **`driver_show_before_ready` يُظهر الطلب قبل جاهزيته**، و«استلمت» معطّل حتى `ready_at` | الطريق للفرع يستغرق دقائق لا يستغرقها الطعام؛ لكن مَن يُرسَل ليستلم غير الجاهز يقف أمام كاونتر بلا شىء |
| 2026-09-21 | **`expected_ready_at` تقدير مستقلّ عن لحظة نداء السائق** | `by_max_item_prep_time` يُقدّم النداء بمهلة الاستباق؛ عرضه كـ«جاهز خلال» كان سيكذب على كل سائق فى كل طلب |
| 2026-09-21 | **انقضاء التقدير حالة رابعة («عند المطبخ»)، لا «جاهز»** | التقدير ينتهى ولا ينتهى الطبخ؛ تسميته جاهزاً تُرسل السائق ليقف منتظراً |
| 2026-09-21 | **نافذة ٦ ساعات وسقف ٥٠ على «المتاحة»** | يستطلعها كل سائق متاح كل ٢٠ث؛ طلب عالق منذ أسبوع كان سيُحمَّل فى كل استطلاع للأبد |
| 2026-09-21 | **`currency` أُضيف للعقد** (الريبوان + المرآة) | السائق قد يعمل لمطاعم فى دول مختلفة؛ رقم بلا عملة رقم لا يُتصرَّف به |
| 2026-09-21 | **🔴 صاحب المشروع: مقياس واجهة داخل التطبيق** — نقضٌ صريح لقرار «مقياس النظام يكفى» | إعداد النظام عام لكل التطبيقات ومدفون؛ من يراه كبيراً هنا لن يُصغّر جواله كله |
| 2026-09-21 | **المقياس يحرّك الكثافة لا الخط وحده**، و`TouchTarget.primaryPhysical` يقاوم | تصغير الحروف داخل بطاقات بُنيت لأكبر منها يترك صناديق نصف فارغة؛ وتصغير ٦٤dp يُبطل الرقم الوحيد المجمَّد |
| 2026-09-21 | **٤٠١ على طلب موقَّع يُنهى الجلسة فوراً** (`SessionExpiryInterceptor`) | كانت تظهر «حدث خطأ ما» وزرّ إعادة محاولة لا ينجح أبداً — تطبيق يبدو معطوباً وهو يطلب تسجيل دخول |
| 2026-09-21 | **شاشة «متاح» تجلب سبب «لا طلبات» بنفسها** | كان يصل من تبويب الطلبات، فالشرح الذى يمنع السائق من ظنّ العطل لا يظهر إلا لمن زار تبويباً آخر أولاً |
| 2026-09-21 | **`NoOrdersReason` نُقلت لـ`:core:ui`**، و«مطاعمى» دُمج فى «حسابى» | وحدة feature تعتمد على feature أخرى بداية تشابك؛ وخمسة تبويبات على جوال صفّ أهداف أضيق من الإبهام |

### المزالق (مبذورة من POS — أضف هنا)
- **🔴 «الوحدة تُبنى» ≠ «التطبيق يُبنى».** وحدة `:core:notifications` الجديدة أخذت نفس الـnamespace الخاص بـ`:feature:notifications`؛ كلتاهما تُترجم وتنجح اختباراتها وحدها، والانفجار يقع عند دمج الـdex فى `:app` (`Type … BuildConfig is defined multiple times`). **أى وحدة جديدة تُتحقَّق بـ`:app:assembleMeniuraDebug` لا بـ`compileKotlin`.** (حُلّت بتسمية حزمة النواة `app.qrmenu.driver.alerts`.)
- **🔴 عطل لا يكشفه إلا الجهاز: حقل غائب فى الرد يُسقط ميزة كاملة صامتاً.** كل الباك اند كان صحيحاً (صفّ العرض، القفل، الترشيح) وشاشة العرض لم تظهر أبداً — لأن `DriverOrderResource` لم يكن يرسل مفتاح `offer` إطلاقاً، فلا يستطيع التطبيق تمييز «عرض بمهلة» من «طلب مفتوح». **اكتب اختباراً على السلك (`assertJsonPath`) لا على القاعدة وحدها.**
- **لقطة الشاشة قد تسبق إعادة التركيب بأجزاء من الثانية** — ظهر أن العرض «لا يظهر» ثلاث مرّات وهو يظهر فعلاً. عند تتبّع شاشة تُفتح تلقائياً: سجّل من داخل الـComposable ثم التقط، ولا تحكم من لقطة واحدة.
- **`artisan serve` أحادى الخيط، وسجلّه مُخزَّن مؤقتاً عند إعادة التوجيه** — لا تستنتج «التطبيق لا ينادى الخادم» من ملف سجلّ فارغ.
- **`uiautomator dump` مع Git Bash يحتاج `MSYS_NO_PATHCONV=1`** وإلا حُوِّل `/sdcard/…` لمسار ويندوز.
- **Doze يُفرَض ولا يُنتظَر:** `adb shell dumpsys deviceidle force-idle` يُثبت نجاة خدمة الموقع فى دقيقتين بدل ساعة فى الجيب — وهو **أقسى** لأنه يفرض أسوأ حالة بدل انتظار وقوعها.
- **بلا `JAVA_HOME` يخرج Gradle بلا سطر `BUILD`** — ابحث عن `BUILD SUCCESSFUL` حرفياً.
- **المحاكى يُفسد الأجسام الكبيرة من `php artisan serve`** — `adb reverse` + `127.0.0.1:8000`، و`maxRequests=1` فى debug.
- **`DnsRetryInterceptor` ضرورى على HONOR/Huawei** — أول طلب بعد الاستيقاظ يفشل DNS.
- **`name_ar` nullable فى كل DTO** — شركة/فرع بلا اسم عربى كان يُسقط تحليل JSON فى POS.
- **حفظ ما بعد الدخول داخل `NonCancellable`** — الانتقال للشاشة التالية كان يلغيه.
- **الحقن الجديد يحتاج `hiltJavaCompile…`** — خطأ Dagger لا يظهر فى `assemble` وحده أحياناً.
- **ترحيل Room «الصحيح بالقراءة» ليس دليلاً** — نفّذه على مخطط الإصدار السابق.
- **`FlowRow` لا `Row` لرقائق العربية** — `Row` تكسر النص حرفاً فى كل سطر.
- **عزل اتجاه الأرقام والهواتف والمبالغ** (`BidiText.ltr`) وإلا انقلبت.
- **النقر على نص صف إعداد يقلب مفتاحه** — عند الأتمتة انقر بموقع العنصر.
- **الحسابات الزمنية بتوقيت الشركة لا الخادم.**
- **🔴 جهاز الاختبار (S25) لغته `en-EG` ثم `ar-EG` — أى الإنجليزية أولاً.** فالتطبيق يظهر إنجليزياً افتراضياً وهذا **سليم لا خطأ**. لاختبار لغة بعينها استخدم:
  `adb shell cmd locale set-app-locales <pkg> --locales ar` ثم أعِدها بعد الاختبار. (وهذا بالضبط ما يبرّر قرار **منتقى اللغة داخل التطبيق** مستقلاً عن النظام.)
- **⚠️ `translatable="false"` على `app_name` خطأ** — نحن نترجمه فعلاً. لا تضعه على أى مورد له `values-ar`.
- **🪤 `heredoc` فى أداة Bash يبتلع `\\`** — كتابة `sdk.dir=C\:\\Users\\…` خرجت `C\:\Users\…` فأعطت `IOException: Invalid file path`. **استخدم شرطات أمامية** (`C:/Users/…`، يقبلها Java على ويندوز) أو `printf '%s\n'`.
- **⚠️ `adb shell cat /sdcard/…` فى Git Bash** يحوّل المسار إلى `C:/Program Files/...`. سبّقه بـ`MSYS_NO_PATHCONV=1`.
- **لا تنسخ `res/drawable` من POS بالكامل** — `splash_background.xml` يشير إلى لون لا وجود له هنا فيفشل ربط الموارد.
- **⚠️ جهازان متصلان (الهاتف والتابلت)** ⇒ `adb` يخرج بـ`more than one device`. مرّر الهاتف صراحةً دائماً:
  `ADB -s adb-R5GL32AA1DB-wNDKAe._adb-tls-connect._tcp` (S25، `SM-S731B`، 1080×2340). التابلت `NDL_W09` لا يمثّل تطبيق سائق.
- **✅ تحقّق الثيم بالبكسل لا بالعين** — أدقّ وأرخص من قراءة اللقطة:
  `Image.open(png).getpixel((w//2, int(h*0.35)))` ثم قارنه بحساب `lerp` للتوكن.
  قياس 2026-09-21: الفاتح `#F1EFEA` مقابل `#F3F0EB` المحسوب (فرق ٢ — مطابق).
  الداكن `#0D0B08` مقابل `#15130F` المحسوب: **أغمق مما حُسب، لكنه ليس أسود نقياً ويحتفظ بترتيب R>G>B (دفء العلامة)** — غالباً تعتيم OLED من سامسونج فى الوضع الليلى. **أعِد القياس على الهاتف الرخيص قبل الحكم بأنه خلل.**
- **`CLAUDE.md` فى ريبو POS بائت جزئياً** — إعادة التصميم مدموجة فى `main` رغم ما يقوله. **الكود هو الحَكَم.**
- **🪤 `grep … | head -5 && echo "CRASH" || echo "no crash"` يكذب دائماً** — حال خروج الأنبوب هو حال `head` (صفر دائماً) فيطبع «انهيار» حتى مع صفر مطابقة. **لا تُضِف `head` لسلسلة فحص الانهيار** فى `CLAUDE.md`؛ أو افصل: `grep -c` ثم قارن العدد.
- **🪤 أداة Bash تسقط الـheredoc الطويل صامتاً** — ملف اختبار بـ٢٨٠ سطراً عبر `cat > f <<'EOF'` لم يُكتب إطلاقاً و«فشل» برسالة `unexpected EOF`. **استخدم أداة Write للملفات الكبيرة**، و`python` للتعديلات الموضعية.
- **لا `pyyaml` على هذا الجهاز** — مرآة العقد تُولَّد بـ`symfony/yaml` المثبّتة فى **vendor الباك اند** (استخدام للقراءة فقط). الأمر كاملاً فى رأس `BackendContractMirrorTest`.
- **`Accepted` ليست فى `components.schemas`** بل فى `components.responses` — اختبار المرآة يحتاج `mirrorAgainst` بمخطّط خام لا باسم.
- **`mutableList += JsonArray` لا يُترجم** (يُقرأ كإعادة إسناد) — استخدم `addAll`.
- **🔴 `AppCompatDelegate.setApplicationLocales` لا يُعيد رسم `ComponentActivity` عادية** — لا على API 35 ولا قبله. أى «تبديل لغة فورى» داخل شاشة يجب أن يُبنى بـ`createConfigurationContext` + `CompositionLocalProvider(LocalContext/LocalConfiguration/LocalLayoutDirection)`، لا بتغيير لغة التطبيق. (تغيير لغة التطبيق يلزمه `recreate()`، وهو ما تفعله «متابعة».)
- **🪤 `uiautomator dump` لا يُظهر نصوص Compose بصدق** — أظهر الشاشة البديلة بينما اللقطة تُظهر شاشة اللغة. **اللقطة هى الحَكَم على الجهاز، لا الـdump.**
- **`adb shell uiautomator dump /sdcard/x.xml` نفسه يحتاج `MSYS_NO_PATHCONV=1`** فى Git Bash، لا الـ`cat` وحده — وإلا كُتب الملف فى مسار مترجم ثم «لا يوجد».
- **🪤 AAPT2 يرفض `'` أو `\"` غير مهرَّبة داخل نصّ مورد**، ورسالته مضلِّلة تماماً: `Invalid unicode escape sequence in string`. و`html.escape` يكتب `&#x27;` وهو يُفكّ إلى `'` عارية فيُرفض أيضاً. الصحيح `\'` حرفياً.
- **`const val` ممنوع داخل كلاس اختبار** (يحتاج `companion object`) — استخدم `val`.
- **الجهاز جهاز صاحب المشروع** — لمسة منه أثناء الاختبار تُغيّر الحالة تحتك (حدث فعلاً: ضُغط «متابعة» بين لقطة ودَفعة أوامر). **امسح الحالة صراحةً قبل كل سيناريو** (`run-as <pkg> rm shared_prefs/…` + `force-stop`) بدل الافتراض.
- **🪤 وكيل فرعى نفّذ `git checkout` على ملف خارج نطاقه فمحا عمل وكيل آخر** (`PlanSeeder.php`، تراجُع عن فروق سطور، بينما وكيل ثانٍ كان يكتب فيه بالتوازى). القاعدة موجودة فى «الوكلاء الفرعيون» لكنها لم تكن فى **بريف** الوكيل. **اكتب حظر `git checkout/restore/stash/reset` حرفياً داخل تكليف كل وكيل**، ولا تفترض أنه قرأ `CLAUDE.md` كاملاً. وراجع `git status` بنفسك بعد كل موجة وكلاء: الفرق كان `+129/-0` فظهر الفقد فوراً.
- **🪤 `vendor/bin/pint --dirty` يتجاهل مسارات الملفات الممرَّرة** ويصلح كل ملف متسخ فى الريبو — بما فيه ملفات وكيل آخر **وهو فى منتصف تحريرها**. حدث فعلاً أن حذف سطر `use` كـ«غير مستخدم» قبل أن يُكتب السطر الذى يستخدمه، فصار `FirebasePhoneVerifier::class` يُحَلّ لـ`App\Providers\FirebasePhoneVerifier` ولا يربط شيئاً — **بلا خطأ قاتل، ربط صامت خاطئ**.
- **🪤 `CLAUDE.md` هنا بنهايات أسطر CRLF** — أى تعديل نصّى بـpython/sed يطابق سطراً بلا الـcarriage return سيفشل صامتاً. اقرأ بـ`newline=''`.
- **بصمة SHA-1 ليست اختيارية لـFirebase Phone Auth** — مشروع مضبوط بحزم مسجَّلة و`oauth_client` فاضى = الرسالة لا تُرسل أبداً. افحص `google-services.json` بـ`len(client['oauth_client'])` قبل أى اختبار ميدانى.
- **`google-services.json` مكانه `app/src/<flavor>/` لا `app/`** — ملف واحد فى `app/` يُطبَّق على النكهتين، فيأخذ بناء تاج مشروع منيورا.
- **🔴 طلب صلاحية غير متزامن + بدء خدمة أمامية فى نفس اللحظة = `SecurityException` تقتل التطبيق** على أندرويد ١٤+ (`FOREGROUND_SERVICE_LOCATION` يشترط منحاً قائماً **لحظةَ** البدء). احفظ النيّة وابدأ الخدمة حين يصل المنح فعلاً، **و**التقط الاستثناء داخل `onStartCommand` (الصلاحية قد تُسحب من شريط الإشعارات والخدمة تعمل).
- **🪤 أخضر + مثلث تحذير = لا رسالة.** الحالة المدعومة («مسموح أثناء فتح التطبيق») ليست نجاحاً ولا عطلاً؛ سطح محايد وعلامة معلومة. الأحمر يُحجَز للرفض الكامل وحده وإلا تعلّم السائق تجاهله.
- **`git commit` يفشل كاملاً لو مرّرت مساراً متجاهَلاً لـ`git add`** (`_screenshots`). أضِف المسارات المقصودة فقط.
- **عضو Redis GEO لا ينتهى من نفسه** — `SETEX` على مفتاح النقطة لا يُخرج السائق من مجموعة المرشّحين. أى مسار «توقّف عن التتبّع» يجب أن يُزيل العضو صراحةً.
- **`Redis::keys()` تُرجِع مفاتيح مسبوقة بالبادئة بينما `Redis::del()` تُعيد تسبيقها** ⇒ `del(keys(...))` لا يفعل شيئاً صامتاً. جرّد البادئة قبل الحذف فى تنظيف الاختبارات.
- **`PlanSeeder` يستخدم `firstOrCreate`** فلا يحدّث خطة قائمة — تشغيله «ينجح» ولا يضيف الميزة لأى خطة موجودة. منح ميزة لخطة حيّة قرار أدمن من فورم الخطة، لا سيدر.
- **`php artisan serve --port=8000` قد يصطدم بلارجون**؛ استخدم منفذاً آخر و`adb reverse tcp:8000 tcp:<port>` فيبقى `driver.debugBaseUrl` كما هو.
- **لقطة الشاشة بعد `cmd uimode night yes` مباشرةً تُظهر الوضع القديم** — أعِد تشغيل التطبيق ثم قِس البكسل، ولا تحكم من اللقطة الأولى.
