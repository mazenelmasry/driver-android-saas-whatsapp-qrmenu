<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <!-- No android:icon/meta-data for a default notification channel —
             the messages are data-only on purpose (decision 13), and letting
             FCM build its own notification would fight the full-screen vs.
             heads-up decision that PushHandler hands off to
             :core:notifications. -->
        <service
            android:name=".DriverMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
