<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- :core:location — CLAUDE.md § الموقع فى الخلفية. Sampled only while the
         driver is AVAILABLE; the ladder in :feature:availability requests
         these one at a time, never together. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:name=".DriverApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Driver.Starting">

        <!--
            🔴 WorkManager must NOT self-initialise.

            `DriverApplication` implements `Configuration.Provider` to hand
            WorkManager the Hilt worker factory. If the default initialiser
            below is left in place it runs FIRST, at app-startup, and builds
            WorkManager with the stock factory — after which the provider is
            never consulted and `OutboxFlushWorker` cannot be constructed
            (nothing can inject `TripRepository` into it).

            The failure is silent: the worker simply never runs, so a delivery
            queued offline would sit unsent with nothing reporting why. The
            only way to notice is to look for the drain that never happened.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|locale|layoutDirection"
            android:theme="@style/Theme.Driver.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Permanent, honest notification while sharing location (available drivers only). -->
        <service
            android:name="app.qrmenu.driver.location.DriverLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>

</manifest>
