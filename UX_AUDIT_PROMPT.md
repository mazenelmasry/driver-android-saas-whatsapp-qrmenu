# برومت جلسة تصميم (UX/UI) — تطبيق السائقين

> انسخ كل ما تحت الخط لوكيل فى جلسة جديدة.

---

أنت مسؤول تصميم تجربة وواجهة **تطبيق السائقين**: `C:\laragon\www\driver-android-saas-whatsapp-qrmenu`
(Android أصلى — Kotlin + Jetpack Compose، خمس لغات: ar/en/ur/bn/hi، نكهتان `meniura` و`taaj`).

## اقرأ أولاً (إلزامى قبل أى سطر كود)
1. `CLAUDE.md` كاملاً — وخاصة «بروتوكول بداية الجلسة» و«نظام التصميم» و«خريطة الشاشات» و«المزالق».
2. `.claude/skills/driver-ui-standards/` — معيار الجودة المعتمد لهذا المشروع.
3. `.claude/skills/add-compose-screen/` — أعراف بناء الشاشات.
4. نفّذ بروتوكول بداية الجلسة وأعلن نتيجته قبل أى عمل.

## 🔴 السياق الذى يحكم كل قرار تصميمى هنا
هذه ليست شاشة تابلت ثابتة. المستخدم **سائق على دراجة/فى سيارة**، بيد واحدة، تحت الشمس، وأحياناً والمحرّك يعمل. ومعظم قراراته تُتخذ فى ثوانٍ:
- يقبل عرضاً خلال **٤٥ ثانية**.
- يقرأ عنواناً وهو واقف فى الشارع.
- يضغط «سلّمت» وأمامه عميل ينتظر.
⇒ **الوضوح يسبق الجمال دائماً**، وحجم اللمس والتباين ليسا تفصيلاً.

## ⛔ قواعد المشروع (غير قابلة للتفاوض)
- **ممنوع أى أمر git يغيّر الحالة**: لا commit / checkout / restore / stash / reset / branch / merge / clean. القراءة فقط.
- **ممنوع push.** اترك العمل غير مُلتزَم حتى يوافق صاحب المشروع.
- **ممنوع نصّ حرفى داخل Composable** — كل نصّ فى `strings.xml` **وبالخمس لغات معاً**.
- **ممنوع أى رقم dp/sp حرفى** خارج `Tokens.kt`.
- **ممنوع تقصير الأسماء** (`truncate` / `maxLines=1` على اسم مطعم أو فرع) — قاعدة صريحة.
- **ممنوع `CircularProgressIndicator` وسط الشاشة** · ممنوع `tween()` لحركة يقودها المستخدم · **لا شاشة بلا حالاتها الأربع** (تحميل/فارغ/خطأ/محتوى).
- ممنوع تعديل `CLAUDE.md` أو `openapi/driver.v1.yaml` بلا إذن صريح.
- الوكلاء الفرعيون: **Sonnet 5 حصراً، ٣ كحد أقصى، كل واحد فى وحدة Gradle مختلفة.**

## 🎯 المهمة
ارفع التطبيق كله لمستوى تجارى (المعيار المكتوب فى المشروع: Foodics/Square)، **بلا تغيير أى سلوك وظيفى ولا أى عقد API**.

### ملاحظات صاحب المشروع (نقطة البداية — تحقّق منها بنفسك على جهاز)
1. **الهيدر غير احترافى.** لا يوجد شريط علوى حقيقى موحّد؛ العنوان يظهر كنصّ عائم بمسافات واسعة وجرس على الجانب.
2. **ترتيب المعلومات مقلوب:** لافتات التنبيه (صحة الإشعارات… إلخ) تُرسم **فوق** عنوان الشاشة فى `app/src/main/java/app/qrmenu/driver/SignedInScreen.kt`. العنوان يجب أن يكون أول ما تقع عليه العين.
3. **مساحات رأسية مهدَرة** بين العنوان وأول بطاقة.

### ابدأ من جرد لا من تخمين
راجع **الشاشات الثمانى عشرة** فى خريطة `CLAUDE.md` واحدة واحدة على **جهاز حقيقى** (لا معاينة Android Studio وحدها)، وسجّل لكل شاشة: الهيدر · التسلسل البصرى · المسافات · أحجام اللمس · الحالات الأربع · الوضع الداكن · **الاتجاهين RTL وLTR**.

### أنتج بهذا الترتيب
1. **تقرير جرد** بالمشاكل مرتّبة بالأثر على السائق (لا بسهولة الإصلاح)، بلقطات.
2. **نظام هيدر موحّد** يُطبَّق على كل الشاشات — مكوّن واحد فى `:core:ui` لا نسخة لكل شاشة.
3. **سلّم مسافات وتسلسل بصرى** مُطبَّق فعلياً، مضبوط فى `Tokens.kt`.
4. التنفيذ على دفعات صغيرة، كل دفعة **مُتحقَّقة على الجهاز** قبل التالية.

## ✅ التحقق بعد كل تغيير (لا تقل «تمّ» بدونه)
```bash
export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"
bash scripts/verify.sh          # النكهتان + كل الاختبارات
```
ثم **على جهاز حقيقى** (Samsung S25 عبر adb):
- ⚠️ **شغّل `app.qrmenu.driver.meniura.debug` تحديداً** — تحقّق بـ`pm list packages | grep qrmenu.driver`، وشغّل بـ`monkey -p <package> ...`. نسخة قديمة بلا `.debug` أهدرت ساعة فى جلسة سابقة.
- لقطة **فاتح وداكن** لكل شاشة تلمسها.
- **تكبير خط النظام حتى ٢٠٠٪** بلا كسر تخطيط.
- راجع `logcat` بحثاً عن FATAL/ANR.

## 🪤 مزالق مسجَّلة (كلّفت وقتاً فعلياً — لا تقع فيها)
- **الحشوات (insets):** كل تبويب يطبّق حشوة شريط الحالة داخل `DriverScreenScaffold` الخاص به، فأى شىء يُرسم **فوق** ذلك الـscaffold لا يأخذ حشوة ويُرسم تحت ساعة النظام. طبّقها مرّة ثم **`consumeWindowInsets`** وإلا ظهرت فجوتان.
- **`.ltr()` تُطبَّق على القيمة لا على الجملة.** لفّ `stringResource` كاملاً يثبّت العربية LTR فتقفز النقطة لأول السطر. استخدم `TripMoneyFormat` للمال (يعزل القيمة بنفسه).
- **فاصلة عليا غير مهرَّبة** فى `strings.xml` تُفشل `mergeResources` برسالة مبهمة `Can not extract resource` — هرّبها `\'`. والوحدة وحدها تُبنى بنجاح؛ الفشل يظهر عند دمج `:app`.
- **`stringResource` لا يُستدعى داخل lambda لـ`joinToString`** — استخدم `LocalContext.current.resources.getString`.
- **لقطة الشاشة قد تسبق إعادة التركيب** — لا تحكم من لقطة واحدة على شاشة تُفتح تلقائياً.
- **صياغة صحيحة نحوياً وخاطئة دلالياً لا يكشفها اختبار.** «معك ٤٠ من ٣٠» كانت تُقرأ كخطأ حسابى. **اقرأ النصّ على الجهاز لا فى ملف الموارد.**
- **وحدة جديدة تُتحقَّق بـ`:app:assembleMeniuraDebug`** لا بـ`compileKotlin` (تصادم الـnamespace لا يظهر إلا عند دمج dex).

## 🚧 عيوب معروفة مفتوحة (ابدأ منها)
- شاشة «حسابى» → «حجم التطبيق»: بطاقة المعاينة تظهر **بلا عنوان يفسّرها** فتبدو بيانات حقيقية لا عيّنة.
- المؤقّت فى شاشة التوفّر يعرض **أرقاماً عربية-هندية حتى فى الواجهة الإنجليزية** (بقية الأرقام لاتينية) — تناقض داخل الشاشة الواحدة.
- عنصر شبيه بشريط تمرير على الحافة اليمنى فى عدة شاشات يبدو كأثر رسم.

## ⚠️ ما لا تلمسه
- أى منطق عمل أو استدعاء API أو عقد (`openapi/driver.v1.yaml`) أو مخطط Room.
- منطق `NoOrdersReason` و`CashHoldBanner` و`UnsentActionsBanner` — **الصياغة فيها مقصودة وموثّقة**؛ حسّن الشكل لا المعنى، واسأل قبل أى تغيير نصّى.
- نصوص ur/bn/hi: كتبها وكلاء وتنتظر **مراجعة ناطق أصلى**. لا تعتبرها نهائية ولا تبنِ عليها.

## 📌 فى نهاية الجلسة
اتبع **«بروتوكول نهاية الجلسة»** فى `CLAUDE.md` حرفياً (بإذن صاحب المشروع لتعديل الملف)، واترك كل شىء **غير مدفوع** حتى يوافق.
